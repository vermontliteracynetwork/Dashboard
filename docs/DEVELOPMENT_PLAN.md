# Homeplot — Development Plan (Main Reference Doc)

**This is THE main development plan.** Every future Claudia dispatch and every future planning conversation should start here. It supersedes the earlier one-off `Homeplot_Development_Plan.pdf` (generated once, sent to the teacher, never stored in the repo — the exact problem this file exists to fix: a doc Claudia is told to "reference" has to actually live somewhere Claudia can read it on every dispatch, not a PDF that only ever reached the teacher's inbox).

**Built for:** K-8 students with high support needs (autism, ADHD, dyslexia, communication disorders).
**Owner/teacher:** Kayden, Vermont Literacy Network.
**Stack:** React + React Three Fiber (3D world), Zustand + Supabase (Postgres, Storage, realtime sync), Vercel (hosting, auto-deploy on push to `main`).

## Related documents (read these for deep detail on their topic)

| Doc | Covers |
|---|---|
| [`SIZE_REFERENCE.md`](./SIZE_REFERENCE.md) | Build Mode's unit system (1.0 = player height), the full category size chart, and the audit/fixes applied to it. |
| [`LITERACY_WORKSPACE.md`](./LITERACY_WORKSPACE.md) | The final, teacher-approved design for the Literacy Workspace (Polypad-style open sandbox superseding Phase 1 "Literacy Manipulatives") — tile categories, morpheme web, Montessori grammar symbols, canvas/whiteboard behavior, assignment mode, 5-phase build order. |
| [`LITERACY_MANIPULATIVES_FEATURE_AUDIT.md`](./LITERACY_MANIPULATIVES_FEATURE_AUDIT.md) | Code-review feature audit of the shipped Literacy Manipulatives sandbox (`GrammarSandbox.tsx`) against Polypad's real feature set and the teacher's literacy-specific asks (UFLI grapheme tiles, Morpheme Web, Word Lists panel, Montessori shapes) — what's built vs. not, per-item build-effort estimates, and a recommended next-build order. |
| [`TRANSPORTATION.md`](./TRANSPORTATION.md) | The design spec for the Homeplot vehicle system (cars, boats, trains, planes, drone) — mount/drive/dismount pattern, per-vehicle mechanics, placement rules, sound design, build order. |
| [`DRIVING_UX_RESEARCH.md`](./DRIVING_UX_RESEARCH.md) | Comparable-title research (Minecraft, Mario Kart, Roblox Bloxburg/Adopt Me, Stardew Valley, Animal Crossing, LEGO games) and prioritized GUI/interaction upgrade recommendations for the driving feature's next build layer, beyond the shipped Gas/Brake pedal and bigger-driving-area fixes. |
| [`BOATS_DESIGN.md`](./BOATS_DESIGN.md) | Boats (Transportation Phase 2) design extension — reconciles the as-built car mount/exit pattern into the original spec, comparable-title research for water vehicles, water-body/dock placement rules, and a sub-phased build order. Ready for build. |
| [`NATIVE_GAME_STANDARD.md`](./NATIVE_GAME_STANDARD.md) | Policy for any native/embedded game (uploaded HTML5 or built from scratch) wired to a real question set. |
| [`ASSET_PIPELINE.md`](./ASSET_PIPELINE.md) | How 3D asset packs get added to the Build Mode catalog. |
| [`PERSONAL_FINANCE_SCOPE.md`](./PERSONAL_FINANCE_SCOPE.md) | Scope for the personal-finance curriculum Focus lane. |

**How to use this doc with Claudia:** Part A is what already exists (build on it or revise it explicitly, don't re-propose it). Part B is the open backlog, organized by feature front, each pointing at its own doc where one exists — the best place to start a gameplay-design conversation. Part C is the teacher's own raw notes on the pets system and platformer, preserved close to verbatim. Part D is the standing design/safety/accessibility rules every new feature gets checked against.

---

## PART A — Shipped Features (the platform as it exists today)

Organized into four groups per direct teacher instruction: **Game & Play**, **Educational/Academic**, **ABA/SEL/Behavioral**, and **Platform/Other**. Every individual feature gets its own entry: what it is, exactly how a student or teacher interacts with it, and why it exists. A feature that touches more than one group (pets, for example, are a play feature whose training system is also a real ABA design) is written up in full once, under whichever group is its primary purpose, and cross-referenced by name from the other group rather than repeated.

### GROUP 1: GAME & PLAY FEATURES

#### A1. The 3D World: Town Square

The open-air hub every student lands in after login (not a 2D task list first, by direct teacher instruction after reviewing an earlier version). A student walks their avatar around using a repositionable on-screen D-pad, keyboard arrows, or click/tap-to-walk with a hover-preview marker showing exactly where they'll land. The camera follows behind and above the avatar; a student can drag to look around, use explicit look buttons, and tilt the camera vertically, with movement speed adjustable per student (0.5x to 2x) in Settings. Every wandering character (Player, Neighbors, ambient Townspeople) is scaled to read as a real person (Kenney Mini Character models brought up to human height after an early version had everyone looking like ants on the lawn).

Every building in the square (Bank, Store, Post Office, Welcome Center, Computer Desk, Home, Pet Shelter, Island Dock, Cinema, Arcade, or a teacher-custom role/external link) can be walked up to and opened with a one-tap Confirm card; a "Closed/Coming Soon" role exists so an unfinished building reads as intentional, not broken. Collision is real: buildings block movement using their actual rotated footprint (not a generic circle), so a student can never clip through a wall. A Map view shows an overhead grid with double-click-to-teleport or double-click-to-open-a-building.

Navigation/HUD: a single top-right radial pie menu holds Tasks, What now?, Help/Break, Settings/Map/My Stuff (under "More"), My Home, and Companion, all in one place per direct teacher instruction (a deliberate, teacher-approved override of the general "Help must never be gated behind a menu" rule elsewhere in the app, see Part D). Clicking the player's own avatar opens the same pie ("master menu" behavior). A "What's New" changelog book (see A39) auto-opens the first time something new ships that affects a student.

A computer desk placed in the square is the task entry point (a retro browser-styled frame opens the student's daily rotation), so "go do my schoolwork" is a real in-world action, not a separate app section. The world-object catalog enforces a hard zero-weapons rule automatically. A Wizard ThunderSword prop is the visual/interaction surface for the Tier 3 regulation lock described in A28; Help/calm-down always stays reachable through it even while it's otherwise non-dismissable.

#### A2. Home Room (Private Room)

Each student's own private room, picked from a catalog of real-scaled house exteriors visible on their shared "home" building in Town Square. Opens in a read-only View mode by default with a real Build Mode toggle for decorating. Walls are drawn Sims-4-style (click, drag, release) into multi-room floor plans; doors and windows snap to walls. Students paint their own walls and floors, and place furniture/yard decor from the same catalog Build Mode uses (see A4), scoped privately to their own room only. The Pet Care panel (see A5) lives here: feed/pet/play/rename/set-companion/sell, with feelings-word tags and a save-confirmation flash so an action always visibly registers.

#### A3. Creative Island

A Minecraft-style free-build space with the full teacher-parity object catalog, reachable by boat from Town Square's dock or directly from Home Room. Locked by default; a teacher unlocks it per student (`islandBuildUnlocked`, see A40's Student Manager). Same real collision, View mode, and custom-role support as the rest of the world. Exists specifically so building/arranging (constructionist play, the thing Minecraft Education does well) has a home that isn't scoped to the confines of one student's own room.

#### A4. Build Mode / World Editor

The placement/decorating tool underlying Town Square, Home Room, and Creative Island. Catalog: 1,000+ real 3D models across dozens of packs, each with a generated thumbnail and a real-world size class (1.0 unit = player height; see `SIZE_REFERENCE.md`). A student or teacher places an object with a ghost preview, moves it with a crosshair/D-pad, rotates it in 90-degree steps, and resizes it either with size presets (Mini through Giant) or a free-typed exact number, with a live per-object unit readout. Undo/redo is available; the teacher's shared-world changes go through a Draft to Publish/Discard cycle (so students never see a half-finished edit); a student's own room/island changes are always live. A paint brush and bucket recolor ground, sky, and asset tint, with a half-tile grid for precision. Every asset can carry a custom role (see A1), and a teacher can retint, resize, move, or delete even the game's own original fixed layout (buildings, market stalls, road tiles), not just objects placed after the tool existed.

#### A5. Pets System

A full adoptable-animal system: dogs, cats, small critters, farm animals, birds, aquatic animals, and wild animals (37 species across those categories), each modeled at a real player-relative scale (a Great Dane reads noticeably bigger than a Chihuahua, a fish stays genuinely tiny). Every species is open to every student (no per-student scarcity) unless a future teacher tool locks one down.

**Pet Shelter** (Town Square building): every catalog pet is shown and pettable ("give a pat," a small animation) whether or not a student can afford it. A brand-new student has a one-time free-pet coupon: pick any pet, no cost, ever again. After that, pets are purchased directly with Class Cash, or pulled from the **Mystery Adoption Box** ($200, one open per real calendar day) — see A27 for its variable-ratio design. Up to 4 pets can be owned at once (`PET_OWNERSHIP_CAP`); the Shelter also offers a reward-free **Donate** option (see A35).

**Pet Journal**: a permanent, Pokédex-style log of every species a student has ever adopted, even ones later sold, filterable by category. An undiscovered species shows as a "???" silhouette. This is what makes "collect them all" mean something even though only 4 pets can be live-owned at once.

**Care loop** (Home Room): Food, Social, and Health bars (0-100), each paired with a plain-language feelings word at low and high values (e.g. Food low = "Hungry," high = "Full") rather than just a number. Stats decay softly, only while the student is actively in Town Square (never while they're away), floored at 20 so a pet is never neglected to zero, and pets never die. A student feeds, pets, and plays with their pet from a care card; a pet can be renamed anytime and sold back (with a confirm-tap safety) if a student's 4-pet home is full.

**Companion/follow mechanic**: a pet unlocks "walk beside you" in Town Square only after 5 logged task completions (`PET_FOLLOW_TRAINING_THRESHOLD`), tracked by the same counter used for the ABA shaping ladder (see A26). Training comes exclusively from finishing real assignments and question sets, never from clicking care buttons repeatedly, so the companion unlock reflects genuine engagement rather than idle spam. Only one pet can be the active companion at a time.

**Growth stages**: a pet renders visibly smaller as a Baby (0-4 training), Juvenile (5-9), and full-size Adult (10+), using the same training counter, so "raising" a pet from puppy to adult is a real, free visual payoff of ordinary schoolwork, exactly per the teacher's original spec ("pets should start out as babies... and grow to be full adults with attention, love, and native games").

**Color customization**: a 12-swatch paint-brush picker on the pet's Home Room care card (the same palette Build Mode's own paint tool uses) recolors that pet's own model wherever it renders, in Home Room and following as a Town Square companion. Free, uncapped, reversible any time, purely cosmetic autonomy.

#### A6. Transportation System (Cars, Boats)

Full design in `TRANSPORTATION.md` and `BOATS_DESIGN.md`. Any placed car-model object is driveable: click to mount, real steering/gas/brake pedal physics (acceleration, braking, coasting friction, a real turning radius), click again to dismount with a confirm step. A dismounted car parks exactly where it was left. Boats use the same mount/dismount confirm-card pattern ("Board the boat?"/"Get off the boat?") but reuse the ordinary D-pad/WASD as throttle-and-turn rather than a dedicated pedal control surface (the design doc's explicit "no new control surface" rule), and stay within painted water via a gentle bump-and-slide boundary rather than a hard wall. Not yet shipped: wake/splash effects and engine sound (no audio assets exist yet); boats and cars aren't placeable on Creative Island yet; trains, planes, and a drone are designed but not started.

#### A7. Cinema

A pure watch-for-fun video room, reached by walking up to the Cinema building. A teacher stocks it (YouTube links or direct Supabase uploads, see A40's Game/Cinema Videos manager); the student browses a Netflix-style horizontally scrolling "Now Showing" poster shelf, with search, tag-chip filtering, an estimated runtime shown before pressing play, and a heart to favorite a video (favorites sort first, everything else keeps the teacher's own add order). Unlimited replay, no completion tracking, no mastery attached; a video a student should be graded on watching belongs on a real Video task (A17) instead.

#### A8. Arcade (Scratch Games)

A Cinema-styled browse-and-play screen for MIT Scratch (scratch.mit.edu) projects, built on direct teacher request ("my students are obsessed with Scratch"). A teacher pastes a public Scratch project URL; the Arcade shows its real Scratch-hosted thumbnail and plays it inline via Scratch's own official embed, with the same shelf/search/tag/favorite pattern as Cinema. Pure play-for-fun, unlimited replay, no tracking.

#### A9. Music & Radio

A shared, teacher-authored music library (title, YouTube link, tags). A student starts music by clicking the Concert Hall building, a placeable Boom Box prop, or a radio button that only appears while driving a car. Playback is always audio-only (a visually hidden YouTube player, never a video surface). A Spotify-styled Now Playing bar gives real play/pause, previous/next (cycling the whole library), a draggable seek bar, a volume slider, and the same tag-chip filtering the picker uses. Music automatically stops when a student exits the car it was started from.

#### A10. NPCs, Dialogue, Mailbox & Passport

Two tiers of non-player characters: 4 named **Neighbors** (Scout, Penny, Pip, Wren) tied to a launch "Meet the Neighbors" quest, and ambient **Townspeople** who wander the square with no quest attached. Every NPC has a real multi-exchange branching conversation, several complete variants per character (not one fixed script), and a distinct assigned text-to-speech voice. Talking to an NPC shows the exchange as a phone-style chat log (NPC left, student right), with the student picking short response options rather than just tapping Continue. Repeat visits deliberately surface a different variant so conversations stay fresh (a direct novelty-decay countermeasure); once every joke a character knows has been heard, the system falls back to a rotating pool including a plain, joke-free option too, since "not all communication needs to be jokes" was a direct teacher instruction.

Certain punchlines are tagged as jokes: the first time a student reaches one, it's permanently banked into their **Joke Book**, a real page-turning book (CSS 3D page flip, not a flat list) viewable from the backpack inventory (A11), alongside a **Friends** list of every NPC ever talked to.

**Mailbox** (Post Office building): shows the themed item each Neighbor "sends" the first time a student meets them, so the Post Office isn't a walkable-but-inert building. **Passport** (Welcome Center building): a Town-Hall-style summary of the student's own identity, streak, Neighbors met, jokes collected, and earned badges, an Animal-Crossing-style equivalent to Scout's "shows you around" role.

#### A11. Avatars & Emotes

A student picks and customizes their in-world character from a catalog of avatars (purchased with Class Cash, worn instantly) and a catalog of emote images that pop up over their head (Happy, Love It, Great Job, LOL, Sad, Frustrated, Idea, and more). Six of the emotes are free starters, including all four that name a genuinely hard feeling (Sad, Frustrated, Heartbroken, Grr): a deliberate design choice so a student having a hard day doesn't have to pay for the word for it while every positive feeling is free. A backpack **Inventory Hotbar** (opened from the pie menu's "My Stuff") shows everything a student owns without the full shop attached: characters, emotes, the Joke Book, Friends list, and pets.

#### A12. Marketplace

The unified cosmetic/power-up shop, reached by walking up to the Store. Tabs: Characters (avatars), Emotes, Writing (fonts and text/highlight colors for Notes), Whiteboard (marker colors), Voices (read-aloud voice skins), Prizes (teacher-defined real-world or in-game rewards, redeemed by showing the screen to the teacher), Power-Ups (Skip Passes, which let a student cross one non-required task off their list without doing it), Pets (a redirect to the Pet Shelter, A5), My Stuff, and Receipts (a full purchase history). Every purchase goes into a real shopping cart first, with a running total and balance check, before a final Confirm Purchase; a receipt screen afterward shows what was bought and the new balance. A student who can't afford something sees exactly how much more they need, never just a disabled button. See A31 for the optional "Count It Out" checkout mode and A32 for the cart's needs-vs-wants reflection prompt.

#### A13. Daily Spin Wheel

A real spinning prize wheel (rendered with a physics-accurate spin library, not a fake animation), available once per real calendar day. Every spin is a guaranteed win across 10 segments: a Skip Pass, a cashback percentage (3% or 5% of balance), one dedicated pet slot, four distinct cash amounts, and three random marketplace items, all deterministically the same for every student on a given date (so it's fair, not exploitable) and freshly rerolled the next day. If a student lands on something they already own, or their pet home is already full, it silently falls back to a small cash consolation rather than a true dead spin. Finishing the whole day's assignment (both subjects) earns a second **Bonus Spin**, now defaulted on for every teacher who hasn't explicitly turned it off.

#### A14. Quiz Theme Picker

A small "change my quiz's look" button inside any quiz activity, letting a student pick a visual skin (Standard, Pixel, Adventure, Fantasy) for the quiz screen's chrome. The choice is saved to the student, not just the session, on the explicit design principle that predictability matters more than novelty for this population: a themed quiz should look the same every time a student opens it, not re-skin itself randomly.

#### A15. The Platformer Game

A native, from-scratch side-scroller (Blooket-style: gameplay wraps a real question set, see A24 for the retrieval-practice rationale and the Native Game Standard). A student picks a character skin, then runs, jumps, and dodges spike hazards and gaps across 4 increasingly difficult levels (tighter ground, more spikes, wider gaps, and a faster player as levels rise) while collecting coins. Three starting hearts; falling in a hole costs a heart. If all hearts are lost, recovery is a fixed gauntlet: 5 correct question answers in a row (not partial credit, and independent of how many hearts were lost), any miss resets the streak to zero. A question interrupt fires automatically at least once a minute of active play, even with zero mistakes, so retrieval practice keeps happening regardless of how well the platforming itself is going. In-game coins convert to real Piggy Bank money the moment the activity finishes. Touch controls use pointer capture so a held button can't accidentally drop mid-press, a real accessibility fix for imprecise touch input.

---

### GROUP 2: EDUCATIONAL/ACADEMIC FEATURES

#### A16. The Task Engine & Daily Rotation

The core unit of academic work is a **Task**, assembled by the teacher into a student's daily **Rotation**, one list per subject (Math, Literacy) per day. A task can be marked required (can never be skipped with a Skip Pass), daily (repeats every day, starred in the library), or a **Final Check** (completing it marks the whole subject done for the day and triggers the streak/Playground unlock, instead of requiring every other task to also be checked off). Tasks display as **numbered stepping-stones**: any task with an explicit order number must be completed in ascending sequence; once every numbered task is done, every remaining (unordered) task unlocks as a free-choice **Choice Board**, pick anything in any order. A Final Check is automatically forced to the very end of the sequence regardless of how the teacher ordered everything else. Each task can carry a reference image/link, a teacher-authored or auto-generated visual step-by-step guide ("What do I do?"), and its own reward (money by default, or a specific free marketplace item, a one-off custom prize, or a bonus spin). A task must actually be opened before its checkbox becomes tappable, so nothing can be checked off sight-unseen; checking one off always asks "are you sure?" first, and unchecking asks the same in reverse.

#### A17. Task Types

Every content shape a teacher can assign, each its own screen:
- **Quiz**: one question at a time, multiple choice (with optional shuffled questions/answers and teacher-set images with real alt text), matching, or fill-in-the-blank with typo tolerance. Answering freezes on the current question with a clear right/wrong result before advancing; a wrong answer gets requeued to come back around rather than just marked and dropped. A question missed 3 times in a row is permanently retired for that attempt and flagged to the teacher's Review Inbox (a "Quiz Struggle") rather than silently disappearing.
- **External Link**: opens in an in-app browser frame; since most real external sites (Amplify, Polypad, YouTube, etc.) block being shown inside an iframe, the default behavior is an honest "here's where you're headed" card with an explicit tap to actually leave the app, rather than a broken embedded frame. A teacher can flag specific embed-friendly URLs (like Scratch's own embed links) to play truly inline instead.
- **Off-screen/Paper**: a plain-language instruction card for work done away from the screen, with an optional required photo upload of the finished work before it can be checked off.
- **Video**: a themed movie-theater-framed YouTube player; "I watched it!" only becomes tappable once the player itself reports the video played through to the end, not just on a manual tap.
- **Reading Passage + Questions**: a passage (with an optional image) read in place, then flows straight into an attached Quiz.
- **Flashcard Drill**: front/back cards a student flips through and self-checks.
- **Word Chain**: a word-ladder style chain of hint-and-answer steps.
- **Sentence Editing**: a student retypes/corrects a broken sentence to match a target, with an optional hint.
- **Article Reader**: a real web article, cleaned of ads/nav/site chrome, with student-adjustable font size/line height, tappable text highlighting in distinct colors per open tab, and per-highlight notes.
- **Sentence Builder**: a colored-slot graphic organizer (colourful-semantics style) where each blank keeps a consistent color for its sentence role across every organizer a student ever sees, so color becomes a reliable structural cue rather than decoration; connector words the teacher fixes (like "because," "and") sit between blanks automatically.
- **Pick One**: 2-4 video/link options a student freely picks between, never required to do more than one.
- **Platformer Game + Quiz**: see A15/A24.

#### A18. Question Sets, Content Library & CSV Import

A searchable, filterable library of reusable Question Sets (quiz or drill kind), each with a cover image and tags, browsable as a card grid. A full editor lets a teacher build a set by hand, or upload a CSV in a fixed template format that both inserts straight into the activity being edited and saves as a new named set in the library in one step, so nothing has to be entered twice.

#### A19. Common Core Standards Picker

A curated reference set of official Common Core State Standards (CCSS) for Math and English Language Arts/Literacy, grades K-8, sourced and trimmed from the official standard text (not paraphrased), used when a teacher tags a task or a Focus (A20) with a specific standard code. A standard code a teacher puts at the front of a Focus's own detail text is automatically stripped before that text is ever shown to a student, since the code itself is teacher reference, not student-facing content.

#### A20. Focus Sets / Curriculum Spotlight System

Two related systems, both teacher-authored:

**Literacy Focus Sets**: a per-student weekly window of phonics patterns, morphemes, and spelling practice words, shown as a quick reference while the student works on Literacy.

**Focuses**: a class-wide curriculum spotlight across four independent lanes (Math, Literacy, Social-Emotional, Personal Finance), one active focus per lane at a time. A Focus is deliberately never shown to a student as "your weak spot" or "you need to work on this"; it surfaces as a quiet "This week in our classroom" banner on relevant screens (Piggy Bank, Marketplace, subject dashboards), and its word list occasionally (about 1 in 3 conversations, never every single time) drops naturally into NPC small talk instead of an explicit on-screen callout. This framing is a direct, explicit design guardrail against surveillance/deficit framing for a population where an on-screen "target" can land very differently than intended.

#### A21. Activity Library, Plan Templates & Weekly Schedule

A reusable Activity Library (create an activity once, drag it into any student's plan, which copies it fresh rather than linking it live, so editing the library original later never breaks an already-assigned copy). Plan Templates save a whole named daily plan for reuse. A Weekly Schedule maps a template to a specific subject and weekday per student, auto-loading fresh every matching day; an Assignment can instead span a date range, loading once and carrying the student's progress forward across the whole window. A daily plan itself moves through a Draft, Upcoming, Active, Deleted lifecycle in the teacher's Assignments view.

#### A22. Score History & Mastery Tracking

Every full pass through a quiz (every question answered correctly at least once) logs a record: which task, when, how long it took, and how many were answered correctly on the very first try. A teacher reviews this per student in Score History. Questions a student is genuinely struggling with (3 misses in a row) surface privately in the teacher's Review Inbox rather than just disappearing from the quiz, so a real difficulty never goes unseen just because the retry cap protected the student from getting stuck on it in the moment.

#### A23. Literacy Manipulatives / Grammar Sandbox

An open, unscored, un-gated exploration sandbox for sentence grammar (the first shipped piece of a larger planned Literacy Workspace, see `LITERACY_WORKSPACE.md`). A student drags color-coded word-class tiles (naming words/nouns in yellow, action words/verbs in coral) from a sidebar onto an open dot-grid canvas; a noun and verb that agree in number "click together" when dropped near each other, the one rule the sandbox quietly enforces. No lessons, no scoring, no completion state, no reward, by direct teacher instruction: "just open exploration." Reuses the same Whiteboard drawing tool as the rest of the app rather than a second drawing engine.

#### A24. Native Games & the Native Game Standard

Governed by `NATIVE_GAME_STANDARD.md`. A native game (the Platformer today, plus a planned pipeline for uploading third-party HTML5 games from sites like itch.io) exists specifically to make retrieval practice (being asked to recall an answer, not just review content, one of the most robust findings in learning science) tolerable and motivating for a student who would disengage from a plain quiz: the game is the wrapper, the question set underneath is the actual point, and gameplay always serves the question set, never the reverse. Hard rules that apply regardless of which game: a question break, once started, has zero time pressure (no countdown, no answer time limit, full access to every accessibility tool); replay is unlimited (no play-count cap, no daily limit); leaving mid-game always shows a confirm dialog first, and confirming forfeits that session's progress and reward (but never erases mastery already logged before leaving); every completed session generates a full report to the teacher's inbox in addition to the normal mastery record. A teacher configures how often a question break becomes eligible and how many questions appear per break.

#### A25. Playground / Free Play

An ungraded, no-to-do-list space a student can use once the day's real assignment is finished (see A28 for exactly when it unlocks) or during a teacher-granted timed break. A student can free-play any teacher-flagged Playground activity, or pick any saved quiz-kind Question Set to run as a standalone, ungraded Platformer session (A15) with no checkbox waiting on it. Finishing a Playground activity still pays into the real Piggy Bank exactly like a real assignment would (a direct teacher request), just without touching streak, badges, or pet training, since Playground content is deliberately not tied to a specific day's required plan.

---

### GROUP 3: ABA/SEL/BEHAVIORAL FEATURES

Grounded in the standing principles in Part D: no leaderboards or cross-student comparison anywhere, reinforcement schedules follow real ABA principles (continuous reinforcement for shaping, variable-ratio mechanics designed to never produce an empty outcome), regulation is never gated behind currency or "good behavior," and pets/the economy never punish absence.

#### A26. Pets' ABA Shaping Ladder & Growth Stages

Cross-reference: the pet catalog, adoption, and care loop are described in full under A5 (Game & Play). The training system underneath is a deliberate successive-approximation shaping design: a single counter (`trainingProgress`), incremented only by genuine task completions (never by tapping care buttons), gates three visible milestones shown as badges on the pet's care card: "Walks with you" at 5 completions (the companion/follow unlock), "Best Friends" at 10, and "Bonded for Life" at 15. The same counter also drives the pet's visible growth from Baby to Juvenile to Adult, so the reinforcement isn't abstract, a bonded, mature-looking companion is the literal, visible shape of consistent academic effort. Because training only ever moves forward from real work, "I taught it a trick" and "it's the thing I check on first" (both named directly in the original teacher brief) are earned outcomes, not shortcuts.

#### A27. Mystery Adoption Box's Variable-Ratio Design

Cross-reference: reachable from the Pet Shelter (A5). A $200 purchase that always yields a real pet, no empty or "nothing" outcome ever possible. This is the deliberate, load-bearing line between a genuine variable-ratio reinforcement schedule (uncertainty about *which* pet, a fun surprise) and a loot-box mechanic (uncertainty about *whether* you're reinforced at all, which this platform will not build). Rarity is weighted by category (common species pull far more often than rare/ultra-rare ones), and a pull preferentially rerolls toward species the student hasn't discovered yet, so a pull rarely feels wasted. Capped at one open per real calendar day to prevent a same-sitting repeat-open loop.

#### A28. Focus Guardrail Tiers

A tiered system (Claudia's guardrails design) that keeps a student's real assignments visible and inviting inside an open, highly gameified world, without ever gating or blocking the world itself, regulation, or free-roam play. Every tier has a per-student teacher override.

- **Tier 0, Arrival Card**: once per real day, a student lands on a genuine three-way choice: start Math, start Reading, or free time first, with "free time first" a real one-tap option, not a hidden escape hatch. Teacher-toggleable off per student.
- **Tier 1, Count It Out**: see A31 (a financial-literacy skill-building toggle, not a guardrail against distraction, but grouped under this same per-student-override design pattern).
- **Tier 2, Desk Glow**: while a student has unfinished tasks for the day, the computer desk (the task entry point in Town Square) gets a subtle visual glow/label. Teacher-toggleable off per student.
- **Tier 3, Companion Check-In & Wizard Lock**: after roughly 15 minutes of genuinely idle free-roaming (tracked by real movement, not session time) with real tasks still open, a trained companion pet (if the student has one following them) gently surfaces once per session, a dismissible suggestion, never a block, never required, and costs nothing if ignored. Separately, Scout (one of the Neighbors) has a similarly gentle, once-per-session check-in variant available after the same idle window. If a student instead goes a full 30 minutes with real tasks open and makes zero actual academic progress (not just idle time, genuine time without finishing any question set or native game, even while actively exploring or chatting), the **Wizard ThunderSword** appears as a real, non-dismissable lock on general gameplay, the one intentionally hard guardrail in this system. Even then, Help/calm-down (A29) stays reachable through it, per the standing rule that regulation is never gated. The lock clears itself automatically the instant real progress resumes, never on a timer or a dismiss tap.

#### A29. Take a Moment (Calm-Down/QuietTool), Help Ping & Break Request

The regulation path, reachable from the same fixed spot on every screen (folded into Town Square's pie menu, plus a floating button everywhere else), and never gated behind currency, streaks, or task completion. Opening it shows a breathing visual (a circle that grows on the in-breath, shrinks on the out-breath) with optional looping calm ambient sounds (ocean, waves, bubbles, rain), and, if the student has a bonded companion pet, that pet appears as a passive comfort presence, strictly opt-in and only for a student who already has one. A "🐾 Check on your pet" button also appears whenever the student owns any pet (not only a bonded companion), offering a trip to Home Room as a real, optional regulation-break activity rather than just the passive presence line, the explicit Phase 5 tie-in between pet care and regulation tools. From here, a student can quietly ping their teacher for help (which raises a full-screen, impossible-to-miss alert on the teacher's own screen, see A38) or request a break (which a teacher approves or grants for the student's own configured break length, temporarily unlocking the Playground, A25, for that timed window). Both are one tap, never a form, and the tool always confirms what happened ("your teacher has been quietly let know") rather than leaving a student wondering if anything registered.

#### A30. Piggy Bank: Real-Money Economy & Savings Goal

A real dollars-and-cents currency ("Class Cash") a student earns by completing tasks, badges, streaks, and Playground activities, and can view as a full running register (every transaction, income and spending, with icons and dates) or as charts over time. A **streak interest** bonus pays automatically the moment a student's daily streak ticks up: 1% of the current balance per consecutive day, capped at 20%, mirroring how a real savings account compounds, so a longer streak is worth more in a very concrete way beyond the underlying streak badge itself. A **Savings Goal** is entirely student-set (a label like "new bike" and a target amount) and shown as a fill-meter toward that amount; it's concept-only (no real interest math tied to the goal itself), never a teacher requirement, and never blocks spending elsewhere. This exists to make earning and saving legible and motivating (competence and autonomy, in Self-Determination Theory terms) rather than an abstract number going up.

#### A31. Count It Out Checkout

An explicit, opt-in, per-student teacher toggle (off by default) that changes Marketplace checkout from a single Confirm tap into tapping real bill and coin denominations ($20 down to 1 cent) until the tray total covers the price. This targets a specific, concrete skill (recognizing and combining real currency) for a student working on that as a goal, rather than money only ever appearing as an abstract number on screen. Tap-to-add rather than drag-to-a-tray, a deliberate accessibility call: a mis-aimed drag is a much easier miss than a mis-tapped button for a student with fine-motor or motor-planning differences.

#### A32. Needs vs. Wants Reflection Prompt

An optional, non-blocking tap in the Marketplace cart ("Still want it tomorrow? Yep / Not sure") attached to any purchase. It never gates or delays Confirm Purchase either way; it's purely a light reflection prompt, logged alongside the purchase in the register for the student (and teacher) to look back on, building the "needs vs. wants" reasoning habit without turning a purchase into a quiz.

#### A33. Badges & Streaks

Teacher-authored achievements, each either hand-awarded or governed by an auto-award rule (streak reaches N days, lifetime or today's activities completed reaches N, a subject fully finished N times, N Final Checks passed, N distinct tools ever used, N missed-then-corrected questions), each paying its own configurable Class Cash reward on earn. A daily streak (both subjects finished) is the backbone metric multiple systems key off of: it drives streak interest (A30), a "showed up" badge, and is visible on the student's own Passport (A10). No leaderboard or cross-student comparison exists anywhere; every one of these is a personal-progress metric only, per the standing platform rule.

#### A34. Feedback Tool

A structured, quiz-like feedback flow (never a blank "type your feedback" box), built specifically for students whose communication needs, including communication disorders, make free-form typing a real barrier. A student picks a big-icon category (Game Play, Visuals & Design, Assignments & Focuses, Wishlist, or Other), drills down through follow-up icon questions as far as that branch goes (e.g. Game Play to Build Mode to Asset to Add), then explains in their own words, typed or spoken through the browser's own voice-to-text. Every step is one big-icon question at a time, never a form with multiple fields at once. The full path taken (e.g. "Game Play > Build Mode > Asset to Add") is shown to the teacher as a plain-language breadcrumb in the Review Inbox, so context is never lost to a lookup table.

#### A35. Pet Shelter Donation

A real, deliberately reward-free coin sink at the Pet Shelter: a student can donate $5, $10, or $25 of their own Class Cash, with a running lifetime-donated total shown back to them and a simple thank-you message. No coins, items, or pets are ever granted for donating, on purpose, so it can never quietly become a second way to buy something. This is a genuine prosocial/SEL rep (practicing giving, not just earning and spending) rather than another transaction.

#### A36. No Leaderboards / Personal Progress Only

Not a single feature so much as a platform-wide constraint enforced across every system above: nowhere in Homeplot does a student see another student's score, streak, balance, or rank. Every motivational system here (badges, streaks, Score History, Piggy Bank charts) reports only on that one student's own history. This is a direct response to the specific research risk named in Claudia's standing brief: leaderboards are the single most consistently flagged risk factor for unhealthy competition and reduced intrinsic motivation in neurodivergent learners, even when badges, narrative, and other reward mechanics land well with the same population.

---

### GROUP 4: PLATFORM/OTHER FEATURES

#### A37. My Tools Panel & Accessibility Toolbar

A floating "My Tools" button, present on every student screen (and as a smaller inline button inside the internal browser, so tools stay one tap away even inside an embedded external activity). Opens a menu grouped into Subject Tools (Multiplication Table, Hundreds Chart, and Number Line for math; Thesaurus, Dictionary with a visual morpheme/word-parts breakdown, and Sound Wall for literacy) and an Accessibility Toolbar available everywhere: Calculator, read-aloud (TTS) settings including an app-wide dyslexia-friendly font toggle, a Word Processor, a Whiteboard, and the calm-down tool (cross-referenced in full at A29). The **Word Processor** doubles as a Notes app (multiple independently saved, named documents, not one shared scratch blob) with per-word/phrase rich-text color-coding and highlighting drawn only from colors/fonts the student has actually unlocked in the Marketplace, plus a **Personal Journal** mode (the same editor, pre-dated like a diary, direct teacher instruction: "word processor should be base, think of a diary"). Voice-to-text (the browser's own Web Speech API) is available anywhere a student would otherwise have to type at length, including inside Notes and the Feedback tool. A teacher can turn off any individual tool per student in Student Manager, everything defaults on.

#### A38. Teacher-Student Chat & Alert System

A real-time, one-thread-per-student chat between a teacher and student, opened from either side. On the student side, an incoming teacher message never interrupts whatever the student is doing (mid-quiz, mid-drawing, anywhere); it shows as a small, dismissible "new message" toast the student opens on their own terms, an explicit design choice since an unannounced full-screen interrupt is exactly the kind of unpredictable change this population needs protected against. On the teacher side, a student's help ping (from A29) raises a large, impossible-to-miss full-screen alert regardless of which teacher page is currently open, with the next-oldest unresolved ping automatically taking its place once one is resolved.

#### A39. First-Login Onboarding & What's New Book

A one-time walkthrough (plain-language, icon-paired) shown the very first time a student logs in, covering the stepping-stone task flow, the always-available Tools panel, the calm-down button, and the "no rushing, no timers, ever" completion model. Separately, a "What's New" changelog book (the same page-turning book UI as the Joke Book, A10) automatically opens in Town Square the first time something new ships that affects a student, and stays reachable afterward from Town Square, Mailbox, and the computer desk, marking only genuinely-unseen entries as new rather than replaying the whole book every time.

#### A40. Teacher Portal

The full teacher-facing admin surface, all behind its own login: Home/Live Overview (a real-time roster of what every student is doing right now), Student Manager (per-student settings: feature toggles, guardrail-tier overrides, Count It Out, custom tool links, Literacy Focus Sets, Creative Island unlock), Assignments (daily-plan builder, Activity Library, Question Sets, Plan Templates, weekly schedule), a Game tab (Cinema Videos, Arcade/Scratch Games, and Music libraries, split out from academic Activities since all three share the same "play for fun, no tracking" shape), Review Inbox (help pings, off-screen photo submissions awaiting verification, student Feedback submissions, and Quiz Struggle flags, all in one queue), Badge Manager (the auto-award rule engine described in A33), Marketplace Manager (the full cosmetic/prize/power-up catalog), Piggy Bank admin (per-student balance adjustments, streak overrides, and the same register/charts view a student sees), World Editor/Build Mode (A4), Focuses authoring (A20), Student Live View (a read-only mirror of exactly what one student currently sees), Score History (A22), and the same real-time chat as A38.

#### A41. Data, Sync & Platform Infrastructure

Supabase (Postgres, Storage, and realtime subscriptions) as the backend, with a Zustand client store that keeps a local, persisted copy of everything so the app stays usable through a flaky connection. Every write retries with backoff automatically; a write that still fails after every retry surfaces a visible, dismissible banner (not just a silent console error) naming the exact table and error so a teacher reporting it has something concrete to relay, with a manual Retry button and a background retry queue that keeps trying regardless. Vercel auto-deploys on every push to `main`, so a shipped fix or feature reaches students without a separate release step.

#### A42. The Computer: Widget Desktop

The screen a student reaches through the in-world computer desk (A1), styled as an actual retro computer/browser rather than a plain app screen: a title bar with traffic-light dots, a fake address bar ("🔒 www.yoglandia.town/my-computer"), and a single always-in-the-same-place "✕ Close, back to Town Square" button. Below that chrome, the body is a Mac-desktop-style widget grid (`.desktop-widget-grid` in `StudentHome.tsx`), not a row of small text buttons: a live analog clock, a calendar card, and icon/launcher cards for Mail, Piggy Bank (balance shown live), Marketplace, Achievements, Daily Spin (outlined when unspent today), Chat, What's New, and Town Square.

Math, Literacy, and Playground are the same widget-card system, not a separate visual language, by direct teacher instruction ("they should all be varrying size cards but as if they were all computer widgets"): Math and Literacy render as large widget cards (`.widget-card-lg`), Playground as a wide widget card (`.widget-card-wide`), inside the identical `.desktop-widget-grid` every icon widget uses, so the desktop reads as one coherent set of Mac-style widgets in varying sizes rather than "widgets plus a different-looking to-do list." **This sizing/grouping decision is settled** and should not be re-split into a separate visual treatment without new, explicit teacher instruction.

Not yet built: drag-to-rearrange and per-student theming (needs new persisted layout/theme state on `Student`, not a UI-only change). See Part B for the next layer on top of this screen: a shared "webpage frame" chrome standard applied consistently to every other screen a student reaches from here (Bank, Marketplace, Cinema, Chat), plus real data widgets for Bank, Cinema, and Chat directly on this desktop.

---

## PART B — Open Backlog, by Feature Front

### Literacy Workspace (superseding Literacy Manipulatives)

Full design in `LITERACY_WORKSPACE.md`. **Note:** the doc's original 5-phase plan below was overridden mid-build by direct teacher instruction ("proceed with only the open sandbox concept, no explicit activities, learning, etc., just open exploration") — `LITERACY_WORKSPACE.md`'s own "Recommended build phase sequence" section documents this override, so it, not the numbered list here, is the source of truth on what "Phase 1" actually means. Corrected status:
1. **SHIPPED (as overridden), Montessori shapes since added back in.** `GrammarSandbox.tsx` — the open Polypad-style sandbox: persistent left sidebar (Sentence Grammar tile category, port of A23's nouns/verbs), toolbar (Words/Draw/Undo/Read board/Clear), and an open canvas (drag-drop tiles with number-agreement snap, or the reused `Whiteboard` component). No scoring, no lessons, no completion state, matching the override exactly. The Montessori shape+color dual system was initially dropped along with the rest of the original Phase 1 plan, but `LITERACY_MANIPULATIVES_FEATURE_AUDIT.md`'s own "Recommended next build" flagged it as the single cheapest real value left on the table (layers directly onto the already-shipped tile/color map, no new data model or persistence needed) — SHIPPED this hour as a small SVG shape icon (large triangle for nouns, large circle for verbs, per the real Montessori convention) rendered inside each `GrammarPieceTile`, a genuinely independent third visual channel alongside the existing color and text label, per the doc's own "color is never the only signal for meaning" rule.
2. **IN PROGRESS.** Word Lists panel: SHIPPED — a read-only "📚 Word Lists" sidebar category pulls the student's active `LiteracyFocusSet` (phonics patterns / word parts / spelling words, teacher-set in Student Manager) and shows it as tap-to-hear reference pills, not draggable tiles, per the doc's own "a lookup panel, not a mechanic" spec. Sidebar categories (Sentence Grammar, Word Lists) are now independently collapsible (click the header, `▾`/`▸` indicator) rather than only the whole sidebar — SHIPPED an earlier hour, since there are now two real categories for the first time and more (Morpheme Web) still to come. "Simultaneous draw+tiles" — SHIPPED this hour, reinterpreted rather than built as literally requested: Claudia's design pass found true pointer-level coexistence (dragging a tile and drawing a stroke from the same canvas at the same time) would force a gesture-disambiguation rule a student has to learn, a real regression against this file's own "one primary action per screen" finding. The scoped alternative that was actually built: when Draw mode is active, the student's already-placed word tiles now render as a non-interactive, read-only overlay on top of the Whiteboard (at their existing coordinates, `pointerEvents: 'none'`), so a student can see and draw around the sentence they built without losing it and without a second live interaction mode. No pointer arbitration, no change to the Words/Draw toggle itself. Still open: multiple saved/named canvases (today's board is unsaved and clears on exit, per the sandbox's own exit-confirm warning). **Two-level Polypad accordion — SHIPPED this hour, direct teacher request with a real Polypad screenshot ("model the collapsible nature of the manipulatives... as closely modeled to amplify polypad as possible").** The sidebar previously only collapsed at the top category level (Sentence Grammar, Word Lists) — every sub-group inside an open category (Naming words, Action words, Phonics patterns, Word parts, Spelling words) was always visible at once. Polypad's own sidebar is two levels deep: a colored top category opens onto a flat list of plain subcategory rows, each independently collapsible, expanding its own tile grid inline right beneath itself (e.g. "Number Frames" open while "Number Tiles and Cubes" and "Number Bars" stay closed, all under "Numbers"). New `SubcategoryRow` component + `openSubcategories` state (`GrammarSandbox.tsx`) reproduces exactly that: every sub-group is now its own clickable row with a chevron, default open (same "nothing hidden on first visit" reasoning the top-level categories already use), independent of its siblings. New `.lm-subcategory`/`.lm-subcategory-header`/`.lm-subcategory-body` CSS (plain rows, not colored buttons — matching Polypad's own visual distinction between the colored top-level Tiles/Geometry/Numbers labels and the plain subcategory list underneath).
2.5. **SHIPPED this hour.** Mad Libs mode: a third toolbar mode (🎭 Mad Libs) alongside Words and Draw. Six fixed sentence templates (`MADLIB_TEMPLATES` in `grammarContent.ts`) built entirely from the existing unscored `SANDBOX_NOUNS`/`SANDBOX_VERBS` pool, no new content. A student taps a tray tile to fill the next empty blank of that word class (any noun fits any noun blank, no correctness check); tapping a filled blank empties it again; "🔀 New sentence" cycles templates. No validation, no scoring, no completion state, no gating of Words/Draw. Dispatched Claudia first for a design pass on whether this reopens the "no explicit activity" override — her read: it doesn't, since the override killed correctness-checking/mastery structure specifically, not all structure, and the teacher's own uploaded curriculum reference (`docs/curriculum-reference/sentence-formulas/README.md`) already frames her "Yoga Comic Sentence Formulas" content as Mad-Libs-style, so this fulfills stated intent rather than fighting it.
3. **SHIPPED this hour.** Morpheme Web: a fourth toolbar mode (🕸️ Morpheme Web) alongside Words/Draw/Mad Libs, built in a new `src/lib/morphemeContent.ts` module per the audit's own file-organization advice. A student picks one of 4 roots (play/help/care/happy), then taps prefix/suffix tiles (un-/re-/dis-, -ed/-ing/-ful/-less) from a tray to attach or detach them; a tap only "connects" (turns green, reveals the real resulting word, e.g. "re- → replay") when that root+affix is a genuine English word, sourced from a small hand-verified combination table, not generated — invalid taps get a brief neutral wobble (a new `.lm-web-shake` CSS keyframe, respects `prefers-reduced-motion`), never a red/wrong color, matching the doc's "no punitive error state" rule exactly. Simplified from the full spec in two deliberate ways given the time budget: tap-to-attach instead of real drag-and-drop physics (reuses the same interaction pattern Mad Libs already established), and each attached branch is validated independently against the bare root rather than validating multi-affix stacked combinations against each other — both still satisfy "simple right/wrong (connects or doesn't) for v1." Etymology info cards (Phase 5) intentionally not included, as scoped. Content is ordinary, everyday English morphology (not a specialized curriculum standard needing outside verification, unlike UFLI's published scope-and-sequence), so no accuracy-verification blocker applied here the way it did for the still-parked UFLI item.
4. **Not started.** Assignment mode (Polypad/GeoGebra-style visibility slicing) + extended Focus/LiteracyFocusSet teacher editor.
5. **Etymology enrichment card — SHIPPED this hour.** Morpheme Web's own spec (Phase 5, deliberately not included when the mode first shipped) made real: each of the 4 roots (`MorphemeRoot.etymology` in `morphemeContent.ts`) now carries a short, plain-language word-history fact, shown in a "📜 Where this word comes from" card beneath the root+branches display, updating whenever a student switches roots. Purely informational — no tap required, no interaction, never gates the tile-attach mechanic above it, matching this sandbox's "no explicit activity" override exactly. Ordinary, well-documented English etymology (Old English "plegan"/"helpan"/"caru," Middle English "hap"), not a specialized curriculum standard needing outside verification the way UFLI's published phonics sequence would, so no accuracy-verification blocker applied here. Seasonal/novelty polish (rotating which roots are "featured," holiday-themed word sets) is still not started.
6. **Sound/letter-level layer — SHIPPED this session (Claudia's tool-survey items 1-4), direct teacher instruction "Yes to all. Push and get them developed."** The real gap Claudia's literacy-tools survey found: every mode above (1-5) assumes a student can already decode — nothing reached sound/letter/blending, which is exactly what Kayden's real students (grades 5-6, performing at early-elementary skill level) need most. New `src/lib/soundContent.ts` (`SOUND_WORDS`, `WORD_SORTS`) plus three new toolbar modes in `GrammarSandbox.tsx`, all ordinary uncontroversial phonics content (regular single-syllable CVC/CVCe words), explicitly NOT a reproduction of UFLI's specific published board/sequence (that stays blocked/unverifiable, see the Literacy Workspace — UFLI grapheme tiles section below):
   - **🟦 Sound Boxes (Elkonin boxes)** — a decades-old, public-domain phonemic-segmenting technique, the survey's single highest-priority item. One box per real sound in the current word (CVCe's silent e correctly gets no box of its own — "cake" is 3 boxes, 4 letters). Tapping a tray letter (the word's own letters, shuffled) fills the next empty box left to right; tapping a filled box empties it back to the tray — the exact "tap to fill the next blank" interaction Mad Libs already proved, reused rather than inventing a new one. No correct/incorrect gate, matching this sandbox's standing no-punitive-feedback rule; "Read my boxes" is how a student checks their own work by ear.
   - **🧱 Blending Board** — the build-and-blend half of the same skill: tap the next letter (in spelling order this time, not scrambled — blending is about the growing sound, not finding the right letter) to add it to the word, and the growing string is spoken back after every addition so the blend audibly gets closer to the real word. A real, honest limitation logged in the code: browser text-to-speech reads an isolated single letter by its NAME ("cee"), not its phoneme ("/k/") — there is no way to get a true isolated phoneme sound from ordinary TTS, so this leans on reading the growing built STRING back (an imperfect but real approximation) rather than ever claiming per-letter phoneme accuracy.
   - **🗂️ Word Sorts (Words Their Way-style pattern induction)** — tap a word card, then tap the bucket it belongs in; two sorts to start (short vowel sound, all 5 vowels; CVC vs. CVCe/silent-e), each sort's word tray filtered to only words that actually have a real home in that sort's buckets (a bucket-less word in a sort activity was caught and fixed as a real design bug before shipping — the first draft's 3-bucket short-vowel sort left short-e/short-u words with nowhere correct to go). Placement is never blocked (any word in any bucket, matching the sandbox's open-exploration rule), but a real-match placement gets the same positive-only glow the noun/verb snap already uses — self-checking by eye, never a red "wrong."
   - **🖍️ Mark, always available** — direct teacher instruction: "Markers can always be used, even when manipulatives are active." A toggle (not a mode swap) layers a transparent drawing canvas over whatever mode is currently showing — tiles, sound boxes, sort buckets, anything — reachable in one tap from anywhere without losing whatever's currently built. Reuses the exact owned-marker-color/equipped-color data the Whiteboard component already reads, but is a separate, purpose-built transparent canvas rather than Whiteboard itself (which paints an opaque white page background by design, correct for its own Notes use elsewhere, wrong for a see-through overlay here). Same "toggle, don't blend gestures" resolution already used for Draw mode's tile overlay — true simultaneous tile-drag-and-draw was ruled out earlier in this same file as a real gesture-disambiguation risk, and that reasoning still holds; this just makes reaching the toggle itself feel closer to "always there."
   - **Not yet done from Claudia's 9-item plan:** expanding Morpheme Web with the real DSF Level 1 Unit 1 content and the full 22-formula Sentence Formulas Template 7 set (plus its separate 4-step progressive-build scaffold). Word Matrix and the Montessori grammar-symbol/sentence-frame items were built this session too, see item 7 below — reprioritized ahead of schedule by a direct teacher follow-up with real reference images.
7. **Word Matrix (jigsaw pieces) + Montessori Sentence Builder — SHIPPED this session, direct teacher follow-up with 4 real reference images ("puzzle piece like format for morphemes," Montessori sentence-symbol decision tree).** Two new toolbar modes, new `src/lib/montessoriGrammar.ts` content module:
   - **🧬 Word Matrix.** The still-open Word Matrix item from Claudia's build plan, built with the exact jigsaw-piece visual her reference image showed (interlocking prefix/base/suffix pieces) instead of a plain grid. New `PuzzlePiece` component (`GrammarSandbox.tsx`): rather than a true bezier tab/notch outline (real arc-sweep math that's easy to get backwards without a live render to check against, and this sandbox can't screenshot itself), it gets the same recognizable silhouette a more robust way — a plain rounded rectangle body, a small solid circle straddling the connecting edge for a "tab" (always reads as a bump outward, regardless of arc direction, because it's additively drawn, not cut), and a small canvas-background-colored circle straddling the other edge for a "notch" (reads as a bite taken out, since it visually erases part of the rectangle underneath). Reuses the exact same real, hand-verified root/prefix/suffix/combo pool Morpheme Web already validates — same content, a genuinely different manipulative and interaction (two independently-validated flanks around one base, per Claudia's own v1 scope call), not a reskin.
   - **🧩 Sentence Builder (Montessori decision tree).** A real constrained sentence-building engine using the actual Montessori grammar-symbol shapes/colors already read from her Level 1 reference sheet (black triangle=noun, small blue triangle=article, dark blue triangle=adjective, red circle=verb, green crescent=preposition, orange circle=adverb, purple triangle=pronoun, pink rectangle=conjunction, gold cone=interjection — `MONTESSORI_WORD_CLASS_INFO`). Her own words, built exactly as specified: "the words selected in the beginning of the sentence dictate the next words, so that a word can always be added but that it will always make a real, correct sentence in English." `GRAMMAR_STATES` in `montessoriGrammar.ts` is a real finite-state decision tree over English sentence structure (subject phrase with optional article/adjectives → verb → optional adverb/object-phrase/prepositional-phrase/conjunction-into-a-new-clause) — at every point, tapping a shape only ever offers the word classes that are grammatically valid right there, and opens a real dropdown of actual words of that class (subject-verb agreement enforced automatically: choosing a singular subject filters the verb list to singular forms, reusing the exact hand-verified `SANDBOX_VERBS` pool from `grammarContent.ts`). A sentence is marked "already complete" (`canEnd`) only at states where stopping right there is genuinely grammatically valid — a student can always keep going, or stop, and either way the result is always a real sentence, never a fragment. New word banks (articles, adjectives, common/proper nouns kept deliberately separate so an article is never double-counted, subject vs. object pronoun forms, adverbs from her own real Montessori reference list, prepositions, conjunctions, interjections) are ordinary, uncontroversial vocabulary, not a specialized curriculum standard needing outside verification. **Known simplification, not a bug:** sentence capitalization isn't applied to the joined text (e.g. "Wow! the dog runs." reads lowercase after the interjection) — cosmetic polish, flagged rather than silently left unmentioned, not fixed this pass given the size of everything else shipped today.
8. **Speech-to-text removed app-wide; highlighted-text read-aloud added — SHIPPED, direct teacher instruction: "Remove STT, but allow TTS for the items or things highlighted by the student."** STT (the browser's `SpeechRecognition`/`webkitSpeechRecognition` API, used for spoken dictation into text fields) is now gone from all three places it existed: the word processor's "Talk" mic button in `ToolsPanel.tsx`, the feedback flow's dictate-instead-of-type mic button in `FeedbackTool.tsx`, and note-dictation in the article reader's highlight-note editor in `ArticleReader.tsx`. `src/components/MicButton.tsx` and `src/lib/useVoiceToText.ts` are deleted outright (confirmed no other references anywhere in `src/` first). Read-aloud (TTS) is untouched everywhere it already existed, and `ArticleReader.tsx` gains a new capability specifically for the text a student has selected or highlighted: a "Read it" button appears next to "Highlight" the moment text is selected (reads just that selection, no highlight required first), and a "Read this part" button in the highlight-note editor reads back an already-saved highlight's exact text. Both reuse the existing `speak()` helper (`ReadAloud.tsx`) and the student's own TTS settings, no new speech engine.
9. **Sentence Formulas — SHIPPED, Claudia's audit + expansion, direct teacher instruction: "The sentence formula development... should be developmental priorities" and "Claudia should Audit the sentence formula concept and expand."** Claudia's audit found the shipped Mad Libs mode (item 2.5 above) was NOT a real implementation of Kayden's own Sentence Formulas Template 7 curriculum — only 2 of 6 real slot types existed, none of the 22 named formulas existed, no phonics leveling, no progressive 4-step scaffold. Her expansion plan is what got built, in a genuinely new `src/lib/sentenceFormulas.ts` module and a new "📐 Sentence Formulas" toolbar mode (kept separate from Mad Libs, which stays exactly as-is for open silly random play — per her mechanism call, a curated NAMED set of target structures is the whole point here, so it's built on the same click-a-blank-to-open-a-word-bank picker pattern the Montessori Sentence Builder already uses, not folded into that tool's open-ended decision tree):
   - **All 22 real formulas across her 8 categories** (Basic Action, Descriptive, Adverbial, Object, Existence/Location, Command/Request, Question, Negative), transcribed from the real curriculum taxonomy, each with two worked examples using her own recurring characters (Yoga, Azalea, Xander, Geoff, Moxie, Ms. Kayden). Auxiliary/function words (Do/Does, is/are, is not/are not, does not/do not) render automatically once WHO is filled, correctly conjugated, never a student-fillable blank, matching Claudia's own "auto-render, not student-fillable, no correctness check" call.
   - **The separate 4-step progressive build** (WHO+ACTION → +WHAT → +WHERE → +WHEN), a real "🌱 Build a Sentence" toggle alongside the 22 named formulas, not folded in as a 23rd formula, since its point is the step reveal, not a target structure to name and repeat.
   - **Phonics-tiered word banks (General/CVC/VCe/Blends)** for WHO, WHAT, WHERE, and DESCRIPTION (adjective) — Claudia named this the single biggest functional gap in the old pool ("today's word pool is one-size-fits-all"), directly serving her students' real early-elementary decoding level regardless of their grade number. Reuses existing hand-verified content rather than re-authoring: `PROPER_NOUNS`/`SUBJECT_PRONOUNS`/`ADJECTIVES`/`ADVERBS` from `montessoriGrammar.ts`, `SANDBOX_VERBS` from `grammarContent.ts`, real CVC/CVCe nouns from `soundContent.ts`. **Known scope trim, logged rather than silent:** WHEN/FREQUENCY/DESCRIPTION-adverb stay general-tier only, real time/frequency words and -ly adverbs are inherently multi-syllable, so forcing a CVC/VCe version would be unnatural, not genuinely easier.
   - **Montessori shape convergence, exactly per Claudia's recommendation:** each blank shows its Montessori shape/color (`MONTESSORI_WORD_CLASS_INFO`, no new palette) as a secondary badge, with her own WHO/ACTION/WHAT/WHERE/WHEN/DESCRIPTION semantic label staying the primary text, since the two systems are semantic vs. syntactic and don't map 1:1, shape stays secondary, never a replacement for the label her own explicit-instruction sequencing depends on.
   - **Toolbar regrouping, Claudia's own fix, shipped alongside rather than after.** Her audit flagged the toolbar's 9 flat top-level modes as already past this doc's own "max 5-6 visible nav options" standard, and warned that adding Sentence Formulas as a 10th flat button would make an already-named violation worse. Fixed with one level of grouping (no nested dropdown deeper than this): Select/Draw stay always-visible base tools; everything else clusters into "🔤 Sounds & Words" (Morpheme Web/Sound Boxes/Blending Board/Word Sorts/Word Matrix) and "📝 Sentences" (Mad Libs/Sentence Formulas/Sentence Builder), each a toggleable second toolbar row that shows the active mode's own label on its group button even when collapsed, so a student always sees where they are.
   - **A real documentation error Claudia caught and this pass fixed:** this doc previously said "26-formula" in three places (a stale count from before the real taxonomy was fully read), corrected to the real 22 formulas plus the separately-tracked 4-step progressive scaffold throughout.
10. **Orton-Gillingham Sound Wall — SHIPPED, direct teacher instruction: "I want a ORTON GILLIHNHAM sound wall that is interactive. When each phoneme is clicked, a window should pop up showing the list of graphemes to spell that sound, an image of accurate mouth placement, word list containing that phoneme, and a close to return to sound wall."** Lives in My Tools (`soundWall`), not Literacy Manipulatives, rebuilt in place rather than moved, since it was already a standalone tool distinct from the sandbox. New `src/lib/soundWallData.ts` replaces the old flat alphabetical `SOUND_WALL` (`wordData.ts`, left in place but no longer read by the component) with the real OG convention: consonants grouped by manner of articulation (stops, nasals, fricatives, affricates, liquids, glides) and vowels grouped by mouth position (short, long, r-controlled/"Bossy R", diphthongs, other), not alphabetically by letter, the entire point of a sound wall over a plain alphabet chart is that one sound can have several spellings, grouped together. Tapping a phoneme tile opens the exact popup specified: ways to spell it (every real grapheme), mouth placement, a tappable word list (each word speaks on tap), and a Close button back to the wall grid, plus a "Hear this sound" button. **"An image of accurate mouth placement" was substituted with an honest, accurate TEXT description of tongue/lip/teeth position and voicing** (ordinary articulatory phonetics, not a specialized/copyrighted curriculum needing outside verification, same "ordinary vs. specialized-and-unverifiable" line drawn everywhere else in this doc) rather than risk a wrong or fabricated image for exactly the students this tool serves, logged here as a real substitution the teacher can add a genuine photo/illustration on top of later, not a silent downgrade of her request.

### Transportation System — driving robustness fix

- **Direct teacher report, "driving is not functional" — one real bug found and fixed by code audit, cause not fully confirmed live.** Traced the full click-to-drive path (vehicle detection, confirm card, mount, gas/brake physics, exit) end to end in `TownSquare.tsx`; the mechanism is sound for the ordinary case. One real gap found: a car/boat's onClick ternary checked `obj.role` (the generic "View X?" building popup) BEFORE checking `isVehicle` — any vehicle that also happened to carry a role (accidental, or left over from an earlier edit) would silently show a dead "View X?" card with no matching `ROLE_VIEWS` entry instead of ever offering to drive, with no error and no visible explanation. Reordered so a vehicle always drives regardless of any role it carries. This sandbox can't render 3D to confirm this was THE cause of the teacher's specific report — if driving is still broken after this, it needs a more specific description (can't find/click a car at all? click does nothing? car doesn't move once mounted? controls don't respond?) since nothing else in the code path showed a defect on read-through.

### Transportation System

Full design in `TRANSPORTATION.md`; UX upgrade ideas in `DRIVING_UX_RESEARCH.md`. Recommended build order: Cars → Boats → Trains → Planes, with a Drone added alongside Planes (same mechanics/camera/controls as the plane, per direct teacher instruction). **Phase 1 (Cars) is shipped** — see A6. **Boats (Phase 2)**, designed in `BOATS_DESIGN.md` (4-part sub-phased build order: 2a core loop → 2b sound/VFX → 2c placement tooling → 2d accessibility closeout) — **2a (core loop) and a placement-warning slice of 2c are shipped**, see A6. Still open: 2b (wake/splash VFX and engine/splash SFX — deliberately deferred, no audio assets exist yet and this environment can't live-test sound) and 2d (shared sound/reduced-motion toggle, also still open for Cars).

- **Gas gauge — rebuilt to a 10-dash, time-based, questions-only-refuel spec, refined across several direct teacher follow-ups, overriding this doc's earlier "never punitive/decorative-only" ruling.** Gas meter (10 dashes) drains 1 dash per **15** real seconds of driving/idling in a car (a full tank empties in exactly 150 seconds) — deliberately not gated on the gas pedal or on actually moving, just on being mounted — and persists across getting out of and back into the same car (`TownSquare.tsx`'s own state, not inside `Player`, so mount/dismount never resets it; only a fresh login does). The drain timer fully pauses (not just visually) the moment a gas question is on screen — voluntary Fill Up or the forced lockout alike — direct correction: "while answering questions to get more gas, the gas in the tank should be paused, and not decrease any more"; it resumes exactly where it left off once the modal closes. It only refills via correctly-answered questions at the gas pump: a real MC question at random from the teacher's own Question Sets library, +1 dash per correct (capped at 10), 0 for a wrong answer — no partial credit. Voluntary "Fill up" (gauge button, opens any time before empty) is optional and closable. If dashes hit 0 without refueling first, a blocking lockout opens automatically: student must answer 10 questions correct **in a row** (any miss resets the streak to 0) with no dismiss/skip/backdrop-close, before driving resumes — `gasBlocked` stops the car dead in `Player` while locked out. Claudia flagged streak-reset-to-0-on-a-miss as the single highest dysregulation-risk parameter in the mechanic for this population and named it on the record rather than silently softening it — kept exactly as specified at the teacher's explicit direction. Two mitigations Claudia did sign off on: a miss during lockout reads "Let's try the next one!" (never "streak reset"), and the running streak count stays visible ("You're at N of 10") through a miss. **No longer auto-tops-up when the library is empty — see the new auto-question generator entry below**, which now supplies a real fallback question instead. **`gas-pump` role, direct teacher request ("give asset role for gas pump")**: a teacher can place a model and assign it the Gas Pump role from Build Mode's role dropdown, same as Bank/Store/Cinema/etc; clicking a placed Gas Pump opens the exact same refuel prompt as the HUD's Fill Up button/the empty-tank lockout, right where the student is standing, instead of navigating to a 2D route (`ROLE_VIEWS['gas-pump']` is intentionally never read). Two models now carry this role: the catalog's existing "Gas Station" (`suburb` category) and a new teacher-uploaded, purpose-built "Gas Pump" model (also filed under `suburb`, `public/world/models/suburb/Gas_Pump.glb`) — a Sketchfab embed link she pasted first couldn't be used (view-only, no downloadable file, unverifiable license), so she exported and uploaded the real GLB directly instead. The HUD Fill Up button and the automatic empty-tank lockout both still work from anywhere while driving regardless of whether a pump is placed.
- **Steering fix — SHIPPED, direct teacher bug report ("right and left controls when driving are backwards").** Both the car and boat steer by incrementing `facing` directly rather than deriving it from a movement vector like walking does, and the sign was backwards — verified against the chase-cam math (`camAngle = facing`, screen-right = `-X` when `facing = 0`): pressing D/right was turning the nose left on screen. Fixed for both vehicles. Cars briefly picked up a reverse gear (Brake at a standstill reverses) per "model driving controls after Mario Kart," then the teacher corrected it in a follow-up: **"brake should not reverse. just stop the car."** Reversed back out — Brake now only ever decelerates a forward roll to a stop, `carSpeed` never goes negative, no reverse gear exists.
- **Collision: every placed object blocks movement now, not just role-having ones — direct teacher correction superseding the earlier trees/rocks-only fix.** First pass only added `forest` (trees/rocks) to `COLLIDING_CATEGORIES` for newly-placed objects; the teacher's follow-up was broader: "all assets that have a role... cannot be driven through, but currently the other assets can. fix so no assets can be driven or walked through." `TownSquare.tsx`'s collision-layout builder and `IslandBuild.tsx`'s obstacle list both now include every placed `worldObject` unconditionally — the `collides`/`role` gate is gone from the filter (the `collides` field itself stays in the data model, additive-only, just unread here now). This deliberately overrides the standing "never retroactively make an old object solid" guardrail from `types.ts`'s `collides` comment, since the teacher's instruction was explicit and general, not scoped to newly-placed objects. `HomeRoom.tsx`'s own room-collision list already had no such gate (every placed item already blocked movement there) — no change needed.
- **Achievements — paused entirely, direct teacher instruction: "clear all achievements, dont give anymore and dont add any without me saying it. that will be updated later."** `BADGES_PAUSED` (top of `store.ts`) is now `true`, and both `awardBadge` and `evaluateBadgeRules` are real no-ops while it's set — covers rule-based auto-awards AND the teacher's own manual Award button in Badge Manager (now disabled there with an explanatory banner, so it doesn't look like a silent bug). The existing `resetAllStudentAchievements()` action/button in Badge Manager (already built for a prior redesign) is how she actually clears every student's earned badges — this session didn't/couldn't invoke it directly (no live browser here), so it's still pending her own click. Standing instruction for future work in this file: do not add new entries to `DEFAULT_BADGES` (`./badges.ts`) without her asking for them specifically, and do not flip `BADGES_PAUSED` back without her explicit go-ahead to resume.

### Pets — remaining phases of Claudia's 7-phase plan

- **Phase 5 — SEL regulation layer:** SHIPPED. The Home Room care panel shows feelings-word tags, a bonded companion appears as a passive comfort presence inside Take a Moment (strictly opt-in, only for a student who already has one), and Take a Moment now also offers a real "🐾 Check on your pet" button (shown whenever a student owns any pet, not only a bonded companion) that navigates to Home Room as a genuine, non-required regulation-break activity, exactly the explicit tie-in this item asked for.
- **Phase 6 — Companion check-in nudge:** SHIPPED. Stat-threshold-based (any owned pet under 40/100 on food/social/health), delivered as a "(count)" suffix on the Town Square pie menu's Companion wedge (same pattern the Tasks wedge already uses) plus a small heart icon on that pet's thumbnail in the companion-picker wheel. Purely informational, never a popup, never gates anything, and costs a student nothing if they never look.
- **Pet paint-brush/color-fill customization:** SHIPPED. A new `StudentPet.tintColor` field, editable from a 12-swatch color picker on the pet's Home Room care card (the same palette Build Mode's own paint tool uses), with a Reset option to return to the model's original color. Reuses the exact clone-material-and-override-color mechanism `WorldObjectRenderer` already uses for `WorldObject.tintColor`, applied to both places a pet's own model renders: the Home Room presence (`HomePetPresence`) and the Town Square companion-follow model (`PetCompanionModel`). Purely cosmetic, free, uncapped, reversible any time, no teacher decision needed.
- **Companion visible in Home Room before training threshold:** ALREADY SHIPPED (doc was stale). This hour's audit found `HomePetPresence` in `HomeRoom.tsx` already renders every owned pet at baby/juvenile/adult scale (via the same growth-stage system as A26), unconditionally, regardless of training progress — a real 3D presence from the moment of adoption, not gated on the companion-follow threshold. The suggested fix in this line was already built; this entry was just never marked done.
- **Teach a Trick — SHIPPED this hour.** Part C's own open question ("I taught it a trick"? Three different builds) made real: a new "🎪 Tricks" row on the Home Room pet care card, 5 generic tricks (Sit, Spin, Shake, Speak, Play Dead — same set for every pet, no new 3D assets/animations, matching the milestone-badge precedent). Tapping "Teach [trick]" always succeeds on the first attempt (SEL/bonding, not an academic task — no retry-until-correct, no skill check) and permanently marks it learned (new `StudentPet.tricksLearned` field), rendering as a checked badge chip thereafter. New `teachTrick` store action mirrors `tintPet` exactly. Dispatched Claudia first for a fresh audit of what's next now that the Literacy Workspace backlog was largely cleared — she ruled out pet accessorizing (checked the full asset manifest for hat/bandana/collar models: zero exist, a real asset gap not a 30-45 min build) and quest-gated pet unlocks (needs a real quest-to-pet mapping decision) before landing on this as the actually-buildable pick. SQL: `alter table student_pets add column if not exists tricks_learned text[] not null default '{}'` — permalink to follow in the commit message per this repo's standing SQL-citation rule.
- **Pet-for-pet trading — SHIPPED this hour.** Part C's own "pets... can be traded" line, made real by extending the Farmer's Market's async barter mechanism (built two hours prior) to a second owned-item pool. A student picks one of their own pets to offer; the trade always asks for the SAME `PetRarity` tier back (`rarityFor` in `petCatalog.ts`, already existed as the Mystery Box's pull-weighting logic — reused, not invented), never a student's free choice, matching the exact same "no lopsided trades" rule Claudia specified for catalog items. Accepting opens a small picker only when the student owns more than one eligible same-tier pet; with exactly one, it completes directly. A traded pet keeps its name, tint color, training progress, and learned tricks — this reassigns `StudentPet.studentId`, it's not a remove-and-recreate. `following` is force-cleared on both sides of a trade (meaningless for a brand-new owner, and could silently push someone over their 4-pet follow cap otherwise). Reused `FarmerMarketOffer` (extended with a `kind: 'catalog' | 'pet'` discriminant, `wantsPetRarity`, `acceptedWithPetId`) and `acceptFarmerMarketOfferRemote`'s existing same-offer-can't-double-accept race-condition guard rather than building a parallel system — no new Claudia dispatch needed since this applies her already-established fairness rule to a new domain, not a new design judgment. SQL: `alter table farmer_market_offers add column if not exists kind/wants_pet_rarity/accepted_with_pet_id`, permalink `https://github.com/vermontliteracynetwork/Dashboard/blob/e82372b/supabase/schema.sql#L279-L289`. Selling a pet for coins (the other half of "traded or sold") is still not built.
- **Claudia's follow-up review of the pet-trading code, fixed this hour.** Two real findings on top of confirming the race-condition guard, ownership swap, and picker's "always an explicit tap" behavior were all sound:
  - **MEDIUM — a dangling stale offer, fixed.** The existing stale-offer auto-withdraw only checked the just-accepted offer's own poster/item; it missed the case where the ACCEPTING student spends a pet that's the subject of one of their own separate open offers, which would then sit on the board showing a pet its poster no longer owns (not corrupting — the pre-swap re-validation already rejects it — just a confusing dead entry no one would ever explain). The stale-offer sweep in `acceptFarmerMarketOffer` now also covers that direction.
  - **MEDIUM — single-eligible-pet accept skipped confirmation, fixed.** `handleAcceptPet` used to auto-complete the trade with zero confirmation whenever a student had exactly one eligible same-tier pet — unlike every catalog trade, which always shows both exact items before the tap. Now the pet picker always opens, even with one option, so the specific pet being given away is always visible right before it happens.
- **Pet color — "more freedom... mirror after Sims 4," partially answered this hour.** Kayden asked for real Sims-4-style pet painting: a freehand brush with multiple regions/patterns, painted directly onto the coat. That's a genuinely different and much bigger build than today's tint swap — it needs a real per-pet UV-mapped paint canvas (a small texture-painting tool of its own), not a material-color override, and this environment can't visually QA a 3D/texture rendering feature before shipping it (see the Town Square sky section's own standing "no retry without live visual QA" rule, now on its 7th attempt after two confirmed-broken-live ones). Not attempted — flagged here as a real future build, not forgotten. What shipped now, safely, reusing an already-proven pattern: the same native "more colors" picker Build Mode's own tint popover already offers for objects is now on the pet care card's Color row too, so a student isn't limited to the 12 preset swatches — any color, immediately, same one-tap-applies interaction as before.

### Build Mode

- **Paint brush "acts as a bucket," walls unpaintable — fixed, direct teacher report.** Two real gaps found: (1) walls had zero participation in either paint tool at all (`if (paintMode) return; // walls don't participate in paint bucket/brush (no shared model to match on)`) — a click on a wall while painting silently did nothing, which reads as "broken," not "brush." `WallSegment.color` already existed as a field and `WallMesh` already rendered it as the base color, just nothing ever wrote to it — `paintNear` (Brush, radius-based) and a new `paintAllWalls` (Bucket) now cover walls the same way they already covered objects. (2) The "🌱 Ground texture" whole-map bucket-fill sat directly beside the actual radius-based Ground brush (`groundPatch` mode)'s own texture swatches, using an identical swatch-grid look — almost certainly what read as "the brush acts like a bucket," since it's a real one-click, no-radius, fills-everything action. Relabeled "🌱 Fill whole ground with this texture" with a comment pointing at the real 🖌️ Ground brush above it for an actual sized stroke; no behavior removed, just no longer visually indistinguishable from a brush. Floor objects (carpets etc.) already painted correctly through the existing object Brush/Bucket, since they're ordinary `WorldObject`s — no separate fix needed there.
- **Arbitrary texture-on-object painting:** DEFERRED. Today's tools recolor/retexture via tint or a preset ground/sky swap; true arbitrary UV re-texturing of any placed object was flagged early as a later, bigger pass.
- **HomeRoom.tsx's own size system — corrected this hour, not a straightforward fix.** Claudia's audit found the couch/bed/cabinet footprint/cube sizing is a direct, verbatim teacher instruction (`HomeRoom.tsx` lines 28-34), a protected decision, not a bug — `SIZE_REFERENCE.md` §3 previously implied it was an oversight and has been corrected. The real gap: `computeStarterScale` can only hit one target (length OR height) at once per item with today's single uniform `WorldObject.scale: number`, so it can't satisfy both of the teacher's own targets simultaneously the way she specified — fixing that for real needs per-axis (x/y/z) scale storage across every placed-object surface in the app (Town Square/Island/HomeRoom/sync alike), a real schema change, not a small fix. The genuinely open "1.0 = person height" gap is narrower than previously stated: really just the door/window class of items, not furniture sizing.
- **Native-game accessibility vetting:** POLICY DECIDED, TOOLING NOT BUILT. Currently the teacher's own judgment call at upload time; an automated pre-flight checklist could be worth building if volume grows.
- **YouTube duration auto-fetch:** WORKAROUND SHIPPED. Cinema shows a teacher-typed estimate for YouTube links since no YouTube Data API key is configured; uploads already auto-read real duration.
- **Stale `playgroundThreshold` field:** RETIRED from the app this hour. Confirmed it was truly dead (not read by `playgroundAccess.ts`'s real gating logic, and never exposed to the teacher in any editor), so it's been removed from `Student` (`types.ts`), the default new-student object (`store.ts`), and the sync layer's row mapping (`sync.ts`) rather than wired back in — the real unlock rule stays exactly the two paths it already was (both subjects done today, or a teacher-granted timed break). The `playground_threshold` column itself was left in place in `supabase/schema.sql` (unused, harmless) rather than dropped, matching this file's additive-only convention; no destructive DB change was made.
- **Clear All / Clear Room — SHIPPED across every build surface, direct teacher request ("give a clear room/clear all feature that deletes all assets").** Two-tap confirm (tap once to arm, tap again to actually delete — no browser `confirm()` dialog, matches this app's existing destructive-action pattern), never a single accidental tap. Town Square's Build Mode (`WorldEditor.tsx`) and Creative Island's Build mode (`IslandBuild.tsx`) both got a new toolbar "🗑️ Clear All" that deletes every placed object on that surface. Home Room (`HomeRoom.tsx`, already multi-room) got "🗑️ Clear Room" instead, scoped to just the currently active room — clearing one room doesn't touch a student's other rooms or their Yard, matching this file's own per-room model. None of these touch the fixed/original town layout (buildings, market stalls, road tiles) — only teacher/student-placed `WorldObject`s.
- **Creative Island live-mode lag — two safe, zero-visual-regression fixes shipped, direct teacher report.** (1) `IslandBuild.tsx`'s Canvas had `shadows` enabled and the light had `castShadow`, but nothing in the whole file ever sets `receiveShadow`/`castShadow` on an actual mesh (confirmed by search) — no shadow was ever visible, so the entire WebGL shadow-map subsystem was pure per-frame overhead for zero payoff. Removed. (2) Capped `dpr={[1, 1.5]}` on the Canvas to avoid rendering at full 2-3x retina pixel density on an iPad, the single biggest GPU fill-rate cost, and one that compounds with a large student-built object count (Island, unlike Town Square's smaller teacher-curated set, can accumulate a lot of props over a school year). Not fully profiled live (this environment can't run a browser) — flagged as best-effort; if lag persists, the likely next cause is the lack of instancing/batching across repeated placed models (every instance is its own draw call), a bigger follow-up pass, not attempted this session.
- **Fixed, direct teacher bug report: "cannot add role to assets (gas pump), it just closes the menu."** Root cause: the Role picker in the selected-object toolbar (`WorldEditor.tsx`) was a native `<select>` inside a drei `<Html>` overlay on top of the WebGL canvas. A native select's open dropdown renders as OS/browser chrome outside that overlay's own DOM — so the pointerup/click that dismisses it (picking an option) lands back on the canvas underneath at the same screen coordinates, hits the ground plane's own click handler, and deselects the whole object, closing the toolbar before the role change is even visible. Same root cause Claudia's earlier focus-group audit already flagged and fixed for the category filter dropdown in this same file. Fixed the same way: replaced the native select with a same-DOM scrollable button list (matches the tint-swatch picker's own pattern), which can't trigger a stray canvas click.
- **Fixed, direct teacher bug report: "items should be solid on default."** Root cause, Home Room only (Town Square and Creative Island were already universal): `roomObstacles` only ever built a collision entry for a placed object whose model matched the fixed starter/yard `CATALOG_ITEMS` list — a purchased Marketplace furniture item (built for the Marketplace furniture pass above) was never in that list, so it had zero collision, walkable straight through, with no flag anywhere a teacher could set to fix it. Every placed Home Room object gets a real obstacle now regardless of catalog membership — a catalog match still uses its real target-size radius, anything else (marketplace furniture, any future item type) falls back to a generic scale-based radius, matching the "never silently non-solid" rule Town Square/Island's own object collision already follows.
- **Fixed, direct teacher bug report: "students can go through them a little bit and get stuck," now fixed on all three world surfaces (Town Square, Creative Island, Home Room).** Root cause: each surface's movement-collision function only ever checked whether the TARGET point of a move was inside an obstacle; if the CURRENT point ever ended up inside one (two objects placed close enough to overlap, a frame hiccup, a fast vehicle covering more ground in one frame than a walking student), every fallback path bottomed out at "don't move," with no way to ever get back out — a permanent stuck-touching-the-obstacle state. Fixed the same way everywhere: push straight back out to the nearest obstacle's edge first, so "current position" is never itself an invalid one to fall back to, before evaluating the requested move — the same stateless push-out shape `blockBuildings` already used safely for the fixed town buildings.
- **Fixed, direct teacher bug report: navigation icons "highlighting" and breaking driving/walking on a touch screen.** The D-pad, drive Gas/Brake pedals, and camera-look buttons are all hold-to-move controls (`onPointerDown`/`onPointerUp`), but each rendered a visible text caption (and the pedals, an emoji) inside the button. On iOS Safari, holding down on rendered text/emoji content triggers the native Copy/Look Up text-selection callout mid-hold, which broke the gesture the student was actively doing. Fixed two ways: the visible captions are gone (icon-only now, matching every other control in this app, with the accessible name moved to `aria-label`; the Gas/Brake pedals now use the same non-text rotated-arrow icon the D-pad already uses instead of an emoji), and every one of these buttons now also sets `WebkitTouchCallout`/`WebkitUserSelect`/`userSelect: none` directly, closing the callout for good regardless of what content ever ends up inside them.
- **Confirmed already working, direct teacher requirement: "gas pumps can be utilized while driving (no exit car needed)."** Audited the full click path — a gas pump's `onClick` handler (`obj.role && !mapView ? () => setSelectedRoleObjectId(obj.id) : ...`) has no dependency on `drivingObjectId` anywhere in the ternary, and neither does `openRoleObject`'s own `gas-pump` special case. Clicking a placed Gas Pump while driving a car already opens the same "⛽ Get gas?" confirm card and refuel quiz it does on foot — no code change needed, confirmed by reading, not assumed.

### Auto-generated fallback questions

- **SHIPPED (partial, by design), direct teacher instruction: "auto questions should be things like, select the correct spelling pattern (grab ufli scope), answer the multiplication or division fact, complete the subtraction or addition equation, define the word based on the morphemes. lets start with this only."** New `src/lib/autoQuestions.ts`: a generated (not teacher-authored) pool of real MC questions, shaped exactly like `MCQuestion` so anything already able to render one can use it. Four generator types shipped: addition, subtraction, multiplication, and division facts (numbers 1-12, always whole-number results, no per-student grade targeting since none exists in the data model — kept simple per "lets start with this only"), and morpheme-based word definitions, built from the existing hand-verified `morphology.ts` affix-meaning list and `morphemeContent.ts` word set (e.g. "careless" → "without care", composed literally from the real parts, not a polished dictionary definition — that's the actual point of a morpheme exercise). **Spelling-pattern-by-UFLI-scope was NOT built.** Same standing rule as the UFLI blending board entry above: this environment has no way to fetch or verify the real published UFLI scope-and-sequence, and "grab ufli scope" isn't something achievable from here — inventing a plausible-looking sequence and presenting it as UFLI-grounded would be a real instructional-accuracy risk for exactly the students this serves. Needs the teacher to actually supply the real document (same upload pattern the Sentence Formulas curriculum reference used) before this fifth type can be built for real; the module is written so it slots in as a fifth generator with no other changes once that exists. **Wired into one consumer so far:** the gas-refuel loop's `pickGasQuestion` now falls back to `generateAutoQuestion()` instead of silently auto-topping-up the tank when the teacher's Question Sets library has no MC content — a student always gets a real question now, never a free pass and never a broken prompt. Not wired anywhere else yet; the module is written generically (plain exported functions returning `MCQuestion`) specifically so it can be reused elsewhere without rework, if asked.

### Global background music player

- **SHIPPED, direct teacher instruction: "ensure that the music player can always be played in the background, even when students are completing assignments. the music player should have a minimized view."** Previously the entire player (the hidden YouTube audio iframe, the Spotify-style transport bar) lived inside `TownSquare.tsx` and died the instant a student navigated to a task/quiz screen — the exact opposite of "background." Moved to a new `src/components/GlobalMusicPlayer.tsx`, mounted once in `App.tsx` alongside `TeacherHelpAlert`/`CoinDropOverlay`/etc. so it survives every route change; `playingTrackId` itself moved from `TownSquare.tsx` local state into the store (`store.ts`, session-only, not persisted to localStorage) for the same reason. Deliberately kept outside `routes/world/` and importing nothing from `@react-three/fiber` — Three.js stays lazy-loaded for world routes only, per `App.tsx`'s own existing comment on that split; a component mounted for every route can't pull that in. New minimized view: a small "now playing" pill, bottom-left, that never blocks a task/quiz's own content; tapping it expands the existing full transport bar (seek/volume/prev/next/stop), tapping ▾ re-minimizes. Town Square still owns picking a track (the radio/Concert Hall/Boom Box triggers, and the track-browser picker UI) — it just calls the shared `setPlayingTrackId` action instead of owning playback itself.
- **Fixed, direct teacher bug report: "won't let me switch between songs."** Root cause was a missing React `key` on the hidden YouTube-embed component — without one, changing tracks reused the SAME `<iframe>` DOM node (React just updated its id/src), but the outgoing track's own cleanup still calls the YouTube Player API's `destroy()`, which physically removes that iframe from the DOM. That ripped out the very node React had just repointed at the new track, a beat before the new track's effect could look it up by id and attach a player to it — so Next/Prev (and picking a brand new song) silently did nothing once you weren't on the very first track anymore. `key={ytId}` now forces a real unmount/remount per track: the old track's own, no-longer-reused iframe gets destroyed cleanly, and the new track gets its own fresh one. Confirmed already correct and unchanged: the radio is part of the car, so exiting a driven vehicle already calls `setPlayingTrackId(null)` and stops the music (direct teacher ask, already shipped).

### Marketplace — home/Build Mode assets

- **RESOLVED by direct teacher instruction, replaces the parked entry above.** Kayden's own words: "ensure marketplace also has home and furniture items avialable for student purchase. student purchases should unlock and become viewable for them while they are in build mode." This answers the fork this entry was parked on: today's free Build Mode catalog stays free for every student; Marketplace purchases are new premium furniture/home items layered on top, gated by a new persistent `Student.ownedHomeItemIds` (mirrors the existing `ownedAvatarIds` pattern). v1 scope: Home Room's placement catalog only, Creative Island as a fast-follow. Locked items show in the catalog with a lock icon and "Buy in Marketplace" prompt rather than being hidden, matching the Pet Shelter's existing "show it, whether or not they can afford it" precedent. Guardrail: no item that reads as a regulation/calm-down/sensory-comfort object may ever be gated behind purchase, per the standing "regulation path never gated behind currency" rule (Part D).
- **SHIPPED this pass.** New `furniture` marketplace kind, with a "Home item" picker in `MarketplaceManager.tsx`'s New Item form (a real dropdown of already-uploaded interior models from `asset-manifest.json`'s `interior` category — never a hand-typed path a teacher could get wrong). Buying one (`buyMarketplaceItem`, and free-grants via `grantFreeMarketplaceItem`) adds it to the new `Student.ownedHomeItemIds`. `HomeRoom.tsx`'s catalog now merges in every purchasable furniture item alongside the existing free `STARTER_ITEMS`: owned ones are placeable exactly like any starter item (same scale-calibration pipeline, extended to cover them too); unowned ones show locked (🔒 + "Buy in Marketplace", tapping opens the Marketplace) rather than being hidden, per the Pet Shelter precedent Claudia's intake named. Scoped to the interior catalog only, not the yard (furniture reads as an indoor thing) and to Home Room only, not Creative Island (still the noted fast-follow). Editing an existing furniture item's model after creation isn't built yet — delete and recreate for now if the wrong model was picked. **Correction (Claudia's code review caught this): the first version of this shipped a real dead end** — `Marketplace.tsx` had no `furniture` tab at all, so "Buy in Marketplace" led nowhere a student could actually buy one. Fixed in the same pass: a real 🛋️ Home tab (search/category filter, the same buyable-item-card pattern Fonts/Voices/Prizes already use) now exists in `Marketplace.tsx`, and `ownedFieldFor`/`renderBuyableItem` there were extended to recognize the `furniture` kind. The loop is closed end to end now: create in `MarketplaceManager.tsx` → buy in `Marketplace.tsx` → unlocked and placeable in `HomeRoom.tsx`.

### Computer as a real dashboard — webpage-frame standard, Bank/Cinema/Chat widgets

- Kayden's own words, with two reference images (a laptop-bezel mockup of the current computer screen, and a collage of retro Cartoon Network/Nick.com-style web portals plus an OS-window-chrome reference sheet): "i want the coputer screen to look more like attatche image one, where the student can see the computer as if it was a frame for the current view... think of accessing thier computer as the main view and a dashboard to access everything related ot the game that the studnet can interact with. Bank should look like a webpage, with a widget of a chart of their current account. cinema should have a widget. chat should have a webpage looking widget like looks like an old IM page. claudia should take this idea and apply it to all parts of this function"
- Spec: a shared `WebpageFrame` component (title bar, traffic lights, fake address bar showing the real route as a URL, one Back button in the same position on every screen) replacing each screen's own bespoke header, starting with Bank, Marketplace, Mailbox. Three new Computer-desktop widgets, all built on real existing data, nothing fabricated: a Bank sparkline reusing `PiggyBankCharts.tsx`'s already-real balance-history derivation, a Cinema poster-tile widget (most recent or favorited video, static, no autoplay), and an IM-styled Chat widget reusing `ChatPanel.tsx`'s real message data. See A42 for what's already shipped underneath this (the widget-desktop grid itself).
- **SHIPPED this pass.** `WebpageFrame.tsx` built and applied to StudentHome (computer), Mailbox, and Piggy Bank (Marketplace still pending, bundled with its own larger redesign below). Bank widget now shows a real sparkline (`bankDayTotals`, same per-day running-balance derivation `PiggyBankCharts.tsx` uses, just unlabeled and small) plus the live balance — no fabricated history. New Cinema widget shows the real most-recently-added video's real thumbnail (teacher's own `coverImageUrl` or the real YouTube auto-thumbnail), static, no autoplay, "Now Showing." Chat widget shows the real last message in the student's own thread, styled as a small IM-window nested inside the widget-card (`.widget-webpage`/`.widget-card-tall` in `index.css`) — no fabricated unread count, since no read/unread field exists on `ChatMessage` yet.
- **Claudia's code review, fixed same pass:** (1) the Back button had an inline `minHeight: 32` overriding `.btn-sm`'s standard 44px touch target on every `WebpageFrame` screen — removed, the class default applies everywhere now. (2) Collapsing Mailbox/PiggyBank/Marketplace to one Back button lost the old "walked up to this building in Town Square, tap Back, land back in Town Square" path (they all defaulted to the Computer instead) — `TownSquare.tsx`'s own navigation into these screens now passes `state: { from: 'town' }`, which `WebpageFrame` reads to send Back to Town Square instead of the Computer when that's really where the student came from; a caller with its own explicit `backTo` (the Computer's own "Close, back to Town Square") is unaffected. (3) The Bank widget's balance text (`--success` green on white) and the Chat widget's sender label (`--blue` on a light blue bubble) both failed WCAG AA contrast — darkened to `#16803c` and `#1e4fa8`. **SHIPPED, laptop bezel — now on every `WebpageFrame` screen, direct teacher follow-up ("do the laptop-bezel redesign for all webpage screens").** `.laptop-frame`/`.laptop-screen`/`.laptop-deck` in `index.css` originally wrapped only the Computer screen (StudentHome.tsx) in a real physical laptop mockup (dark bezel + keyboard deck) matching the teacher's reference image. Extended this pass to the other three `WebpageFrame` call sites — `PiggyBank.tsx`, `Mailbox.tsx`, `Marketplace.tsx` — reusing the exact same three CSS classes rather than inventing new styling, so all four screens read identically. Decorative only, no interaction; `.laptop-screen`'s `overflow: hidden` only clips content bleeding past its rounded corners (it has no fixed/max height, so it never actually truncates a long page like Marketplace's or Piggy Bank's scrollable history), and every fixed-position modal/FAB still escapes the bezel exactly as before, since neither wrapper div sets a `transform`. **Still not done:** Cinema/Chat/Arcade/Gallery/Farmers Market/Silly Quizzes don't use `WebpageFrame` at all (different chrome patterns each), so they're outside this mechanism unless asked for specifically.

### Marketplace — teacher earn-source editing + Amazon-style browse

- Kayden's own words: "marketplace needs to evolve. as a teacher, in game tab, i need full ability to edit all prices, avaibility windows and resons for earning, etc. for everyting marketplace related. the design needs to be more real onlline shoping inspired, have a search, etc. marketplace needs to be like amazon, still having a website frame that wil be standard and applied to all 'webpages'"
- Spec: new `earnMethods?: EarnMethod[]` field on `MarketplaceItem` ('purchase' | 'daily-spin' | 'bonus-spin' | 'quest-reward' | 'farmers-market'), edited as a chip multi-select per item in `MarketplaceManager.tsx`. Existing items default to all wheel-eligible values on migration (matches today's real unfiltered-pool behavior, no regression); new items default to purchase-only until the teacher opts them into a wheel. `dailySpin.ts`'s segment generator gets a real filter on this field for the first time (today it draws from the entire item list with no eligibility check at all). Characters and Emotes get brought into the same editable price/availability table the other item kinds already have (today Characters have zero per-item editing, Emotes only has a price override). Student-facing redesign: real search bar, keep the existing category chips, oversized "Add to cart" buttons, wrapped in the same `WebpageFrame` standard as the rest of the Computer. **Settled, won't build:** star ratings, written reviews, "customers also bought" cross-sell — real Amazon patterns that either open an unsupervised peer-content surface or add manipulative purchase pressure this population doesn't need; left out deliberately, not by oversight.
- **SHIPPED this pass, the data-model half + Characters pricing + a real search.** `MarketplaceItem.earnMethods` (types.ts) plus `EARN_METHOD_LABELS`/`earnMethodsFor`/`itemEarnsVia` (`marketplaceSeed.ts`) are real now — `MarketplaceManager.tsx`'s `EarnMethodChips` lets a teacher toggle any item between Purchase only/Daily Spin/Bonus Spin/Quest reward/Farmer's Market per item, and `dailySpin.ts`'s segment pool now actually filters on it (`itemEarnsVia(it, 'daily-spin')`) instead of drawing from every eligible item unconditionally like it always has. Characters got the exact override-map treatment Emotes already had (`avatarPriceOverrides`/`setAvatarPriceOverride`, new `CharacterPricesSettings` panel) — a real bug was caught and fixed while wiring this in: `buyAvatar` in `store.ts` was charging the raw hardcoded catalog price even before this pass, ignoring any override entirely (unlike `buyEmote`, which already read from its override map) — now fixed, so a teacher-set Character price is the price actually charged, not just the price shown. A real search box (Characters/Emotes/Power-Ups tabs, which had none; Fonts/Voices/Prizes already had their own via `ItemFilterBar`) and `WebpageFrame` now wrap `Marketplace.tsx`. **Not yet done:** the full Amazon-style layout pass (sidebar categories, bigger product-grid treatment) — today's category-chip tab row was kept as-is per the "it already works, don't reinvent it" guidance, so what shipped is closer to "search added + one shared frame" than a visual redesign; that's still open if the teacher wants the fuller Amazon look.

### Literacy Workspace — UFLI grapheme tiles

- **UFLI blending board — not started, flagged blocker.** `LITERACY_MANIPULATIVES_FEATURE_AUDIT.md` Part 3 already scopes what a v1 needs, starting with "a grapheme dataset... matching UFLI's published systematic-phonics scope-and-sequence." Neither this session nor Claudia (Read/Grep/Glob only, no web access) can fetch or verify the real published UFLI sequence — this environment's network policy already blocked the teacher's own UFLI reference link earlier (`LITERACY_WORKSPACE.md`'s "Note on outside references"). Inventing a plausible-looking phonics sequence and presenting it as UFLI-grounded would be a real instructional-accuracy risk for exactly the students this tool serves. Needs either the teacher supplying the real UFLI scope-and-sequence (a document upload, same pattern as the Sentence Formulas curriculum reference), or explicit sign-off to build from best-available general phonics-sequence knowledge instead, clearly labeled as not verified against the real UFLI text.
- **Kayden's raw feature wishlist for a Literacy Manipulatives rebuild, saved verbatim/described — not yet built, still open under Claudia's discovery-questions document above.** She wants: a real UFLI blending board (referencing `https://www.youtube.com/watch?v=o9hDsQru0bk`, a demo video, and `https://research.dwi.ufl.edu/op.n/file/bca9ju45kvvrvoan/?embed` — attempted to fetch both this session, both blocked by this environment's network egress proxy, same standing block as before; names only, contents unverified); a "UFLI virtual word work mat" (`https://research.dwi.ufl.edu/op.n/file/gc8nkxns914enc7d/?embed`, also blocked, unverified); the format/decision-tree style of the attached **Sentence Formulas Template 7** PDF; Montessori grammar symbols as real manipulatives/reference images; and Montessori "predictable sentence frames" (referencing `https://www.imontesomething.com/blog/Montessori-EFL-Grammar.php`, also blocked, unverified — the two pasted example images were read directly instead, see below). Same standing caution as the UFLI bullet above applies to anything from the two blocked UF research.dwi.ufl.edu links or the blocked YouTube video specifically: their real content can't be verified from here, so nothing should be built as "this is what the real UFLI tool does" without the teacher supplying it directly (a screen recording, screenshots, or a document upload), the same pattern that unblocked the morpheme-tile content.
  - **Sentence Formulas Template 7 — read in full, a real and much bigger spec than what "Sentence Formulas mad-lib mode" currently implements.** The uploaded PDF is Kayden's own actual working document: the full 22-formula Sentence Formulas Template 7 set (across 8 categories), plus its separate 4-step progressive-build scaffold (Basic Action, Descriptive/Linking-Verb, Adverbial, Object, Existence/Location, Command/Request, Question, Negative) — today's mad-lib mode only has 6. Each formula is a color-coded slot row (WHO=yellow, ACTION=green, WHAT=red, WHERE=magenta, WHEN=orange, DESCRIPTION=cyan) ending in a `. ! ?` punctuation-choice column, with worked examples using her own real names and pets (Mom, Dad, "Ms. Kayden," Xander, Geoff, Azalea the cat, Yoga — her own comic character, referenced elsewhere as "Yoga Comic Sentence Formulas") — not generic placeholder names. Word banks are explicitly leveled by phonics stage (a "General" bank, then separate CVC and VCe word banks per slot color) so the same formula can be practiced at an easier or harder decoding level, something the app's phonics-agnostic word lists don't do today. Two blank template layouts exist (Horizontal grid, Vertical grid) plus a linked "Flippity Randomizer" (an external randomizer tool, not something to build in-app — the app's own randomize-a-sentence mechanic already covers that need). A later page ("Question words, articles, conjunctions") shows a harder, silly-sentence tier using her Yoga-comic-universe characters and invented nonsense nouns ("gurgle wurple," "Squirellanida") — a real example of the exact "expanded to ensure high quality" content tier Claudia's discovery document's Question A/F (licensing, grade range) should account for, since this tier is 100% her own original content with zero licensing question, unlike the DSF morpheme material.
  - **Montessori Grammar Symbols — two reference sheets read directly (images pasted into chat, not blocked links).** Level 1: 9 symbols (black triangle=noun, small blue triangle=article, dark blue triangle=adjective, red circle=verb, green crescent=preposition, orange circle=adverb, purple triangle=pronoun, pink rectangle=conjunction, gold cone/teardrop=interjection), each with a full definition and example words — this is the real, standard Montessori grammar-symbol set (large red circle for verb, black triangle for noun, etc. are the actual internationally-standard shapes/colors, not something invented for this request). Level 2 adds 9 more refined subtypes (proper/collective/abstract/spiritual noun variants on the black-triangle noun shape; transitive/intransitive/linking/auxiliary verb variants on the red-circle verb shape; infinitive, participle, gerund) as shape/color only, no definitions given yet.
  - **Montessori "predictable sentence frames" — two example images read directly.** A grammar-symbol-tagged sentence ("Walk down the hallway." / "She dances and sings beautifully." / "He ate on the table." / "A fuzzy cat sleeps.") is followed immediately by its own symbol-tile version directly below it (the same words' shapes/colors, no text, laid out as a strip) — the symbol strip functions as a wordless "parse" of the sentence directly above it, letting a student match words to grammar roles without reading comprehension being the bottleneck. A second image shows a physical (photographed, not digital) version of the same idea for conjunctions specifically: real word cards ("the green crayon," "and," "the red crayon") laid out under matching grammar-symbol shapes, with real crayons placed underneath as the concrete referent — "the green crayon AND the red crayon" / "the yellow crayon OR the blue crayon," teaching a conjunction by physically pairing two concrete objects rather than an abstract definition alone.
  - **Kayden's answers to Claudia's discovery questions — real decisions, protected, logged verbatim/mapped below. Still not built (see the build-order decision at the end).**
    - **Licensing (Q A1/A2), RESOLVED.** Her own words: "All permission for all licensing is given. This is exclusively for private personal use and I have full permission." She is asserting she personally holds the rights/permission to use the DSF Word Origins content (and, by the same statement, any other uploaded curriculum reference) for this private, personal, non-commercial classroom tool. This clears the licensing blocker on the morpheme-tile content specifically — real DSF Unit 1 morphemes, definitions, and passage content can now be built from directly, not just "inspired by the structure." Does not change anything about the UFLI links, which remain blocked for a different reason (this environment's network policy, not licensing) and still unverified.
    - **Sandbox vs. graded/assignment mode (Q B3), RESOLVED.** "Sandbox format unless I have built an assignment (like polypad created activities, think geogebra)." Two real modes, not one: (1) the existing open, unscored sandbox stays exactly that — full unrestricted access ("when in sandbox, all morphemes and otherwise words should be shown"); (2) a genuinely new **teacher-authored Activity mode** (Polypad-Activities/GeoGebra-style: a teacher builds a specific, scoped exercise from the same manipulatives) — inside an activity, "I have ability to turn in [on] specific morphemes," i.e. the teacher restricts which morphemes/content are available for that one activity. This is a real, separate, bigger build (an activity author + player, not just content) — explicitly lower priority, see build-order decision below.
    - **Markers/draw tool availability, RESOLVED.** "Markers can always be used, even when manipulatives are active." The draw/marker tool must stay reachable in every mode, sandbox or activity, never locked out by which manipulative is active. Today's Draw toolbar mode already satisfies the letter of this (Words/Draw/Mad Libs/Web are all switchable, draw is never gated behind another mode being off) — worth an explicit UX pass once sandbox content work above is further along, to confirm marker access reads as "always there," not just technically reachable.
    - **New content type — Word Matrices, RESOLVED to build.** "Licensing is good for morpheme webs and matrices." A word matrix (real structured-word-inquiry convention — a grid showing a base/root in the center with its prefixes and suffixes arranged around it, all the words in the family formed by combining them) is a genuinely different visual/manipulative from the existing Morpheme Web (which is tap-to-attach, single root at a time) — a new sandbox content type to design and build, not a Morpheme Web variant.
    - **Real student level (Q F), RESOLVED.** "My students that are using are grades 5-6 but currently are academically very low so early elementary skills are needed." DSF Level 1 Unit 1 (which reads as an early/entry level on paper) is actually correctly targeted, not too easy — build for early-elementary skill level content regardless of the students' actual grade number. No per-student grade-tier picker needed for this population; one appropriately-easy pool is the right scope.
    - **Spelling patterns, RESOLVED to build broadly.** "All spelling patterns." Read together with the licensing statement above, this is instruction to build out real spelling-pattern content broadly (not just the DSF morpheme set) — the CVC/VCe phonics-leveled word banks already read from Sentence Formulas Template 7 are one real source for this with zero licensing question (her own document); the UFLI blending-board item stays blocked specifically on this environment's network access, not on permission, so it's a separate open item, not resolved by this answer.
    - **Build order, RESOLVED — governs everything above.** "Build all sandbox features before assignments." Every sandbox-mode item (expanded Morpheme Web content from the real DSF Unit 1 roots, Word Matrices, broader spelling-pattern content, Sentence Formulas Template 7's full 22-formula set, Montessori grammar symbols as real manipulatives, Montessori predictable sentence frames) comes first; the teacher-authored Activity mode (the Polypad-Activities/GeoGebra-style restrictable-content layer) is explicitly deferred until the sandbox is done. Next build session should work this list in sandbox-first order, not touch Activity mode yet.

### Town Square sky

- **Full sky texture — 5th attempt (drei's procedural `<Sky>` shader) confirmed BROKEN by the teacher's own live screenshot, reverted to a single flat color.** The shader attempt shipped without a live visual check (this sandbox can't render 3D) despite the file's own standing "no retry without live visual QA" rule — the teacher previewed the branch and the sky rendered as jagged translucent white/blue shards, not a clean gradient, the same class of failure the rule was written to prevent. Direct instruction after seeing it: "one cohesive color of sky texture." `SkyboxBackground` in `TownSquare.tsx` now always sets `scene.background` to a single flat `THREE.Color` (the teacher's chosen `skyColor`, or `#bfe3ff` by default) — no shader, no texture, the same technique `WorldEditor.tsx`'s own Build Mode preview already uses safely. The `<Sky>` import and component are removed entirely, not just made conditional. **This is now the 5th attempt to fail and the 2nd to be confirmed broken by a real look (not just theorized) — treat any future sky-rendering change as needing a live screenshot from the teacher before it ships, not shader-structure reasoning alone.**
- **6th attempt — a real equirect sky texture, direct teacher request, shipped opt-in — CONFIRMED BROKEN live, then fully removed.** "Add the sky textures i've added to build mode for fill sky." Two real uploaded images were made pickable from Build Mode's Fill Sky panel, applied via `EquirectangularReflectionMapping` — the same technique every prior photo-sky failure above used. The teacher's own live screenshot of production (`independent-work-dashboard.vercel.app/#/world/town`) confirmed the predicted failure: large jagged dark-blue shard/triangle shapes scattered across the sky, the same failure class as every attempt before it. Direct instruction: "these dark blue shapes... need to be removed. it should not be there at all." **`EquirectangularReflectionMapping` is now confirmed broken by a real live look on two independent source images in this codebase (the 4th attempt's Kenney skybox and this 6th attempt's teacher-uploaded images) — this is a standing rule, not a per-image one: do not retry this mapping technique again on a new image, in either `TownSquare.tsx` or `WorldEditor.tsx`.** Full removal, not just hiding: `SkyboxTexture`/`BuildSkyTexture`/`SKY_TEXTURE_OPTIONS`/`SkyTextureBoundary.tsx` (the fallback error boundary built for this feature) are all deleted from both files; Town Square and Build Mode's Fill Sky panel now always render a single flat `THREE.Color`, full stop, regardless of what's stored in `app_settings.sky_texture` — the column itself stays in `schema.sql` (additive-only, never dropped) but is never read for rendering again. Build Mode's "🌤️ Sky" panel keeps its flat-color fill + Reset, just without the texture picker underneath it.
- **7th attempt — a real seamless-tileable sky pack, direct teacher upload, shipped opt-in, STILL NEEDS A LIVE TEACHER LOOK.** Kayden uploaded a genuine seamless/tileable 512x512 texture pack (Screaming Brain Studios, CC0, 96 images across 8 cloud styles — Bumpy/Cloudy/Fading/Fuzzy/Gradient/Puffy/Simple/Wispy Sky — x mood), asking that the whole sky show one of these patterns "with no breaks." This is a fundamentally different asset and technique from every prior attempt above, not a retry of the banned one: those were single wide equirectangular panoramas force-mapped with `THREE.EquirectangularReflectionMapping` (confirmed broken twice, real jagged shard artifacts both times); these are small isotropic patterns explicitly built to repeat edge-to-edge with no visible seam, the same idea as `WorldEditor.tsx`'s own `GROUND_TEXTURE_OPTIONS` ground textures, which already tile safely today via `RepeatWrapping`. Shipped the same way: new `SkyDome` component in `TownSquare.tsx` — a large (radius 180, comfortably past the fog's own 90-unit far distance and anywhere the camera actually reaches) `BackSide` sphere with the chosen texture tiled `RepeatWrapping` (6x3 repeats), `fog={false}` so it isn't washed out to flat fog color at that radius. All 96 images copied into `public/world/textures/sky/`, cataloged as `SKY_TEXTURE_OPTIONS` in `townLayout.ts` (shared with `WorldEditor.tsx`, same pattern as `ROLE_VIEWS`). Build Mode's Fill Sky panel got a two-level picker back (category chips, then a thumbnail grid per category) writing to the store's existing `skyTexture` field (already there, unused since the 6th attempt was fully removed — no schema change needed) — picking a thumbnail applies immediately, a "Remove sky texture" button returns to the flat-color default. Opt-in only: `skyTexture` defaults to `null`, so every existing world is unaffected until a teacher actually picks one. **Still genuinely new 3D rendering this sandbox cannot visually verify before shipping — treat this exactly like every sky change before it and get a live look before trusting it**, though the odds are meaningfully better here than prior attempts: the failure mode that broke every equirect attempt (pole/seam distortion from forcing a single non-panoramic image to cover the whole sphere) does not apply to a texture that's deliberately tiling, and a soft cloud/noise pattern (no hard directional lines) tolerates the sphere's own top/bottom UV pinch far better than a photo would.
- **Cinema curtain frame — SHIPPED, direct teacher request after seeing it live.** The Cinema background used to switch to a `curtain-screen.png` image with a fake "screen" rectangle painted into the curtain photo itself, while the real video played in an unrelated fixed 640px box centered by flexbox — the two never lined up, so the video visibly floated next to the painted screen instead of sitting on it. Fixed: `Cinema.tsx` now always uses the plain `curtain-closed.png` (no painted screen), and the real video/title bar are absolutely positioned to match the exact rectangle that used to be painted into `curtain-screen.png` (measured directly from that now-unused image: left 24.53%, top 24.54%, width 50.89%, height 50.83% of the 1920x1080 photo — a true 16:9 box). `curtain-screen.png` is no longer referenced anywhere but was left on disk rather than deleted.
- **Mailbox navigation — simplified per direct teacher request.** Removed the What's New/Town Square/Home three-button row in favor of a single "⬅️ Back" button to `/student/home`, now that Mail is reachable straight from the Computer's own widget grid (this session's Computer redesign) rather than only by walking to the Post Office.

### Investigated this hour, not actionable as code

- **"Delete standing Wizard asset from Town Square"** (open task backlog item): searched the full codebase (`townLayout.ts`, `store.ts`'s default-student/world seeding, all `WorldObject`-placing code) for any statically placed Wizard model — none exists in code. The three Wizard GLB models in the catalog (`blob-wizard.glb`, `wizard-thundersword.glb`, `wizardus-maximus.glb`) are either the Wizard ThunderSword regulation-lock's own thumbnail (A28/A29, an intentional feature, not a stray decoration) or ordinary Build Mode catalog/pet items a teacher or student could have placed live via Build Mode. If a Wizard model is currently standing in a specific student's Town Square, it's a live placed `WorldObject` row in that student's own world data, not something in code — remove it in-game with the Build Mode hammer/delete tool (per A114's fix), or say which student's world it's in so it can be targeted directly.

### Confirmed already shipped (stale task-list entries)

- **"Move floating Tasks button into pie menu as 'Computer'"**: already done, not stale until now — `TownSquare.tsx:2820-2824`'s own comment confirms every floating icon (Help, What now?, Tasks) already lives inside the top-right pie menu, per direct teacher instruction, freeing up the bottom-right corner the D-pad used to need clearance from. Task list entry marked completed this hour.

### General notes for the next planning pass

The task list has tracked every phase of the pets rollout, the World Editor/Build Mode buildout, and the core dashboard as complete except the items above — the platform was close to feature-complete against everything scoped before the Literacy Workspace and Transportation fronts opened up, which is why those two are now the largest single blocks of open work. Treat anything phrased as "superseded" in this doc as intentionally not-a-bug (e.g. an early plan to restrict pets to only cats/dogs was superseded by "all animals can be pets").

---

## PART C — The Teacher's Own Notes & Raw Ideas

Preserved close to verbatim so intent isn't lost in summarization. Status notes in brackets show what's since been built.

### On the pets system — full original brief

> Given verbatim as a same-session instruction: "Claudia's job is to completely build out the pets component…"

> What is the pet actually for? Pet is for game play interest, taking a break (take a break with your pet lets you do pet training interactions). Claudia should research the neuro game Mightier and reference their SEL gaming to how the kids interact with pets. Pets are HIGHLY reinforcing for my students and a complex pet system will motivate them to complete assignments, answer question sets, and interact with the platform. "I taught it a trick"? "It's the thing I check on first"? Three different builds.

**[Status]** The motivation/reinforcement loop is built (training tied to real task completion, milestones, growth stages). The specific "Mightier" SEL-gaming research reference and an explicit take-a-break-with-your-pet interaction mode are not confirmed built.

> Where does the pet live, and who sees it? Pets live at home and with enough training (task completion) they can walk beside the player as they go. Public pets can have some status, like completing tasks can unlock certain pets, pets can be earned in daily and bonus wheels, etc.

**[Status]** Home Room care + Town Square companion-follow: built. **Daily-wheel pets: SHIPPED (doc was stale)** — `dailySpin.ts`'s `getDailySpinSegments` already reserves one guaranteed pet-adoption wedge every single day, seeded deterministically per date; Bonus Spin reuses this exact same segment generator (`store.ts`'s bonus-spin resolution calls it directly), so bonus-wheel pets are covered too. Task-completion-gated species unlocks: still not built — every species is open to every student via purchase, the daily/bonus wheel, or the Mystery Box.

> Decay, neglect — pets should have social, health, and food bars. They are like Sims and only decrease when playing the game, not while gone. Soft need decay. Pets never die but they can be traded or sold. Students can have up to 4 pets each.

**[Status]** Built exactly as specified. **Trading between students: SHIPPED this hour** — the Farmer's Market's async barter mechanism now covers pets too (same rarity-tier-only fairness rule already used for fonts/colors/voices), see the Pets section of Part B for the real shape. Selling (for coins, not barter) is still not built.

> How is a pet acquired, and what does that moment feel like? Marketplace purchase, farmers market, daily and bonus wheel, completing quests, can be traded. Each student should start with a coupon... that allows them to select one of the loaded pets for free, any pet of their choice... This should only happen the first time, otherwise they have to buy or otherwise earn pets.

**[Status]** The free-first-pet coupon is built exactly as specified. Direct-purchase and the Mystery Box are built. **Daily/bonus-wheel pets: SHIPPED (doc was stale)** — see the correction above. Quest-completion pets and farmers-market/trading acquisition are not built.

> Does the pet do anything, or is it a companion? Roughly by build cost: companion (idle/follow/react) → caretaking (feed/pet/play/clean) → customization (name it, dress it) → emotional-regulation object.

**[Status]** Companion and caretaking tiers fully built. Naming and Teach a Trick (SEL/bonding, SHIPPED this hour, see below) are built; dressing/accessorizing is not — checked the full asset catalog, zero hat/bandana/collar/bow/scarf/cape models exist to attach, so this is a real asset gap, not a build gap. The emotional-regulation-object tier (Phase 5) already SHIPPED in an earlier hour, not still open as this line previously implied.

> Can two students have the same pet? Every pet type available to everyone unless specifically gifted by the teacher or earned from assignments or quest completion.

**[Status]** Built as the default. Teacher-gift and quest-earned exclusivity is not built.

> Tell me what to do to get animations for the animals/pets.

**[Status]** A logistics question, not a feature request — shipped pet models do walk/idle-animate (birds/fish float instead, by design).

### On the platformer game — original notes

> 1) In platformer game, if you fall in a hole in the ground, you die and lose a heart. 2) Display hearts in right-hand corner of game view screen. 3) Move the to-do-list floating button to top right corner — right now it blocks the screen. 4) Add X/close button where the current to-do list is.

**[Status]** All four built: hole-falls cost a heart, hearts display top-right, the to-do FAB was relocated then folded into the consolidated Town Square pie menu, and a close button exists on the checklist overlay.

### Population & design context, as given

- Students are K-8 with high support needs: autism, ADHD, dyslexia, and other communication disorders.
- Pets, the economy, and social/reward systems are framed as tools specifically for motivation and engagement with this population, not decoration.
- Standing workflow instruction: all development pushes to both the working branch and `main` so changes reach students as soon as they're ready.

---

## PARKING LOT — Historical Chat Requests Audited, Not Yet Built

Per the standing Intake Protocol (`.claude/agents/claudia.md`): no teacher input should go undeveloped or get lost. This section is the first real pass at it — extracted from a full-session chat digest (471 of the teacher's own messages across this project's history), grep'd for request-signal phrasing, then each candidate individually verified against the current codebase before listing here (most historical requests, it turns out, have already been built somewhere along the way and are correctly NOT listed below — verified, not assumed). Genuinely still-open items, each concrete enough to build directly when picked up:

- **Computer UI — card/widget-based dashboard redesign: SHIPPED, now A42.** See Part A, A42, including the settled Math/Literacy/Playground sizing decision. Drag-to-rearrange and per-student theming are still open, tracked there.
- **Build Mode punch-list — corrected this hour: 4 of 5 items were already done, last hour's audit under-searched.** One detailed teacher message, several sub-items; a second pass this hour (searching the actually-shared `WallMesh.tsx`/`WorldObjectRenderer.tsx` components too, not just `WorldEditor.tsx` alone, which is what missed these the first time) found:
  - **Wall corner/intersection joining: ALREADY SHIPPED**, not a gap. `WallMesh.tsx` extends each wall's rendered box by its own thickness past both endpoints specifically to cover the seam at a corner ("the same corner-overlap trick most grid-based wall systems, Sims 4 included, use"), per that file's own comment citing this exact teacher report.
  - **Click-and-drag rotation: SHIPPED last hour** (see above).
  - **Door/window placement on a wall: ALREADY SHIPPED**, not a gap. `WorldEditor.tsx`'s `DOOR_WINDOW_RE` gate blocks placing a door/window anywhere except snapped onto a wall (`wallSnapped`), with a live ghost preview.
  - **Movement-collision glitch near placed objects: ALREADY SHIPPED**, not a gap. `blockObstaclesSlide` in `TownSquare.tsx` replaced the old radial-push-out-only approach specifically to fix this reported glitch: try the real diagonal step, slide along one axis if that's blocked, or don't move at all this frame — "same 'move the player slightly over or stop all movement' shape... only ever changes position by an amount the player's own input already implied, so it can't glitch," per that function's own comment.
  - **Student-facing catalog mirroring the teacher's, filtered to owned, with a marketplace indicator: NOT a gap — superseded by a later, more specific teacher decision.** `HomeRoom.tsx`'s own header comment documents a direct later instruction narrowing this down to a small fixed 5-item interior starter catalog ("they don't need any of the other things yet, they have not earned them"), with a real earn-to-unlock system *intentionally* not built yet, same "don't build blind" caution used elsewhere. The full-catalog-mirror ask predates this and was superseded, not left undone.
  - **Genuinely still open: showing only a selected asset's exterior (not interior) when viewed from outside a building.** This is a real backface-culling/hollow-shell-geometry question tied to how each specific house-shell model was authored, not a quick UI fix — the kind of graphics change this project's own sky-texture history (4 failed attempts) warns against building blind. Needs either a live visual check per asset before shipping, or Claudia's input on a safer general approach, not a same-hour fix. Direct teacher framing for the whole punch-list: "Sims 4 is my favorite building platform and i want the controls for mac to be the model for our build mode."
  - **Click-and-drag rotation — SHIPPED this hour.** A new 🔄 handle sits beside the existing 90°/15°-step rotate buttons on `SelectedObjectToolbar` (`WorldEditor.tsx`) — press and drag left/right to spin the selected object freely, same screen-space-delta technique (`dx * sensitivity`) the Town Square click-drag camera rotate (task #67) already uses, just applied to `rotationY` instead of the camera, via `setPointerCapture` so the drag keeps tracking even once the pointer leaves the small handle. The step-button rotate stays exactly as it was — this is a genuine alternative, not a replacement, per the teacher's own "as an alternative to the current button-based rotate" framing. Only in the teacher's Build Mode (`SelectedObjectToolbar` isn't used anywhere else in the app yet, so there's no student-facing Home Room equivalent to keep in sync with this).
- **Farmer's Market — barter/negotiation mechanic — SHIPPED this hour (v1), seasonal-appearance half explicitly deferred.** Direct teacher ask: a separate location (distinct from the regular Store) where students trade items with each other instead of paying coins, framed as a way to teach negotiation as a real skill. Dispatched Claudia first since this touches real ABA/SEL judgment (fairness, no comparative "winner," negotiation-skill mechanism for this population) — her spec is what got built. New `FarmerMarketOffer` type/table (`farmer_market_offers`, permalink `https://github.com/vermontliteracynetwork/Dashboard/blob/e47e6cf/supabase/schema.sql#L269-L278`), store actions (`postFarmerMarketOffer`/`withdrawFarmerMarketOffer`/`acceptFarmerMarketOffer`), a new `'farmers-market'` `WorldObjectRole` (teacher assigns it to any placed object in Build Mode, same mechanism Post Office/Welcome Center already use — no new 3D model/asset needed), and a student screen (`FarmersMarket.tsx`) with a Trade Board (open offers from other students) and My Offers tabs. **Deliberately async/turn-based, not live simultaneous haggling** — there is no presence/online-status system anywhere in this app to build a real-time two-student negotiation session on, so a student posts an offer and any other student can Accept it whenever they next look, the same async shape `chat_messages`/Mailbox already use. **Scoped to fonts/colors/voices only** (the app's owned-catalog items, reusing `Student.ownedFontIds`/`ownedColorIds`/`ownedVoiceIds` directly rather than inventing a new generic inventory system) **and same-kind trades only** (font-for-font, color-for-color, voice-for-voice) as the built-in fairness rule Claudia specified — no free-typed haggling, no cross-kind trades, no rarity/value scoring, and a trade is never shown as a scored/comparative outcome anywhere in the UI. **Explicitly deferred, per Claudia's own spec:** the "appears at certain times/seasonal" half of the original ask — there's no existing pattern for a whole *location's* visibility being date-windowed (only per-item, via `MarketplaceItem.availableFrom/availableUntil`), and building one blind in the same hour as the trade mechanic itself risked rushing both; a teacher can informally treat it as "market season" today by choosing when to mention/open the role. Also deferred: counter-offers beyond a straight accept/decline, free-text negotiation messages (a real moderation surface), avatar/prize trading (prizes are real-world teacher-fulfilled redemptions, not re-tradeable), and a dedicated teacher-side trade log/manager screen (the trade board itself is the only visibility right now).

- **Claudia's code review of the Gallery + Farmer's Market, both fixed this hour.** Neither feature had a review pass before shipping (a busy sprint), so this hour was spent auditing and fixing rather than building something new. Findings and fixes:
  - **HIGH — Farmer's Market accept race condition, fixed.** `acceptFarmerMarketOffer` only checked local client state before swapping owned items; two students accepting the same offer within the realtime-sync latency window could both pass validation, both locally trade away an item, with only one upsert "winning" remotely and the loser's item silently changing with no explanation. Fixed with a real conditional remote write (`acceptFarmerMarketOfferRemote` in `sync.ts`, a Supabase `.update(...).eq('id', id).eq('status', 'open').select()`) — only the caller whose write actually finds the row still `open` is allowed to touch either student's ownership; the loser gets "Someone else already took this trade." `acceptFarmerMarketOffer` is now async; `FarmersMarket.tsx`'s accept handler awaits it.
  - **HIGH — three Farmer's Market student buttons used the teacher-only-exempt `chip-filter-sm` class, fixed.** That class deliberately skips the 44px touch-target floor for teacher management lists; it had leaked onto three real student actions (dismiss trade message, Withdraw, Change item) with no other student route doing this anywhere in the app. Swapped to `btn btn-sm` with an explicit `minHeight: 44`.
  - **MEDIUM — stale duplicate offers, fixed.** A student could post two open offers for the same item; once one was accepted, the other would sit on the board forever silently failing for anyone who tried it, no explanation given. `acceptFarmerMarketOffer` now auto-withdraws any other open offer from the same student for the same item the moment one of them trades.
  - **MEDIUM — em dash in GalleryManager's teacher-facing copy, fixed.** Rewritten as two clauses joined by a colon instead, per this repo's standing "no em dashes in student- or teacher-facing text" rule (this was rendered `<p>` copy, not a code comment).
  - Everything else Claudia checked passed: no leaderboard/comparison, plain-language non-blaming rejection copy, no dead-ends, consistent nav, icon+label pairing, and the shipped code matches what both Parking Lot entries above describe.
- **Playground — meme/personality-quiz/mini-game gallery — image and quiz halves SHIPPED, mini-games still parked.** From an early Playground-overhaul brainstorm, referencing the Kinzoo messaging app's kid-facing marketplace as a model: "a gallery where i can add videos and images for the kids to look through... fun things like memes, mini games, and silly personality quizzes." Images: a new `GalleryItem` type/table (`gallery_items`, permalink `https://github.com/vermontliteracynetwork/Dashboard/blob/9b4505d/supabase/schema.sql#L254-L260`), a teacher `GalleryManager.tsx` and student `Gallery.tsx`, both shipped an earlier hour. Deliberately scoped to images only — `Cinema.tsx` already covers the video half exactly.
  - **Silly Personality Quizzes — SHIPPED this hour, after a Claudia design pass.** Dispatched Claudia first since this is a real ABA/SEL tone call for this population: a typical internet quiz sorts a student into ONE labeled "type," which risks reading as a literal label a rigid/anxious student fixates on, and a peer-visible "silly" result still functions as informal ranking even when the app itself never ranks anything. Her verdict: GO, but not that shape — 4+ silly low-stakes multiple-choice questions (favorite snack/sound, never mood/social behavior/personality), 2+ possible outcomes that are always a silly non-comparative object or vibe ("You're a Disco Waffle!"), never a trait or role label. Built exactly to her spec: new `SillyQuiz`/`SillyQuizQuestion`/`QuizOption`/`QuizOutcome` types and a `silly_quizzes` table (permalink `https://github.com/vermontliteracynetwork/Dashboard/blob/7c5d18f/supabase/schema.sql#L291-L305`), a teacher `SillyQuizManager.tsx` (build questions/outcomes, same teacher-authored-static-content shape as Cinema/Gallery), and a student `SillyQuizzes.tsx` (one question at a time, client-side tag tally with a random tiebreak, unlimited "🔀 Take again" retakes, never framed as a "true self" result). **Results are never persisted and never shown to anyone but the student who took it** — no results table exists at all, per Claudia's explicit "this isn't optional" call on the standing no-leaderboard/no-cross-student-comparison rule (PART D). Reached from a new tile in the Playground alongside Gallery. **Claudia's follow-up code review, this hour:** verified faithful to her own spec on the one "not optional" item — read through `pickOutcome`'s tally/tiebreak logic and the full take-flow, confirmed a result never leaves the student's local component state (no push to the store, no network call, nothing in `schema.sql` to persist it to). Also confirmed the teacher form can't save a broken quiz (orphaned options are filtered out before save, minimums enforced). One real gap found and fixed: the teacher quiz-list delete button fired immediately on one click, unlike every sibling manager (Gallery/Cinema), which all use a two-step confirm before a destructive delete — now matches that pattern.
  - **Still not built:** mini games (distinct from the native games already shipped; no scope has been written for what a "mini game" here would actually be, a real design question, not implementation).

**Items checked and found already shipped** (so they don't need to be re-flagged by a future pass): the Minecraft-style coordinate-plane overlay in Map view (`TownSquare.tsx` — direct teacher instruction, fully built), the "!" priority-conversation indicator over Neighbors that only shows for an unfinished assignment and clears on completion (same file — teacher explicitly asked for exactly this, already implemented, including a documented earlier "always on" version that was replaced), the nonfiction article reader with per-tab highlight colors/note-taking/font-size/TTS (`src/components/ArticleReader.tsx`), the Joke Book's page-turn book UI (task #113), the student Wishlist feedback category, and Transportation's Cars+Boats phases (A6).

- **Match-3 (Candy Crush style) minigame with question sets gating between turns — Kayden asked about wiring in an existing Kotlin Candy Crush clone (github.com/TobseF/Candy-Crush-Clone); not buildable as a direct integration.** That repo is Kotlin Multiplatform on the KorGE engine, a completely different stack from this app's Vite/React/TypeScript, with no compatible hook for pausing on a real question between turns short of forking their source and standing up a separate Kotlin/Gradle build pipeline. Claudia's read: the underlying ask (short match-3 bursts broken up by real retrieval-practice questions) is sound and fits this population well, gentler motor/timing demand than the existing Platformer, though it draws on visual pattern-matching rather than movement skill, so it's a different competence channel, not a replacement for A15. Two open options, teacher has not yet chosen either: (1) reuse the existing Platformer + Quiz task type (A15) as-is, since it already delivers "real game, real question set gates between turns," just not match-3 shaped, or (2) scope and build a genuinely new native match-3 minigame in this app's own stack, using the Kotlin repo only as a mechanics reference (match/cascade resolution logic), never as source code. Stays parked until Kayden confirms which path, or declines both.

---

## PART D — Standing Design, Safety & Accessibility Rules

### Regulation & safety

- Help/calm-down (QuietTool) lives inside the Town Square pie menu now (a deliberate, teacher-confirmed override of the earlier "never gated" rule — see A1). Outside that one specific override, the general principle still holds everywhere else: a student's path to help should never require more than the standard navigation depth every other screen uses.
- Zero-weapons rule enforced automatically in the world-object catalog.
- A hard interruption (Wizard ThunderSword) always still leaves Help/calm-down reachable, even while otherwise non-dismissable.
- Reinforcement schedules follow real ABA principles: continuous reinforcement for shaping (pet training only from genuine task completion), and any variable-ratio mechanic (Mystery Adoption Box) is designed to never produce an empty/"nothing" outcome.
- Pets and the economy never punish absence — decay only progresses during active play, floors above zero, pets never die.
- No leaderboards or cross-student comparison anywhere in the platform.

### Accessibility

- WCAG 2.1/2.2 AA is the standing bar: 44×44px minimum touch targets, 4.5:1 minimum text contrast, consistent navigation placement (deviations need an explicit, documented reason).
- Every screen keeps a visible way back to Town Square/Home — a genuine dead end is a real bug.
- Read-aloud (TTS) available everywhere a student reads or types, including reading back any text the student has selected or highlighted. Voice-to-text (STT) was removed app-wide by direct teacher instruction (2026-09-22) — typing/tapping is the only text-entry method now.
- App-wide dyslexia-friendly font toggle; color is never the only signal (always paired with icon/text).

### Copy & tone

- No em dashes anywhere in student- or teacher-facing copy.
- Errors explain what went wrong and what to do next, in plain language.

### Process

- Claudia (in-house game-design/instructional-design/gamification reviewer) audits concepts, mechanics, and shipped code against ABA/SEL research and this platform's standards. She does not build features herself.
- **Every new design doc Claudia produces gets written to a file under `docs/` in this repo, and linked from this doc's "Related documents" table** — a doc that only reaches the teacher's inbox (a PDF, a chat message) is not something Claudia can reference on a future dispatch. This rule exists because that exact failure happened once already.
